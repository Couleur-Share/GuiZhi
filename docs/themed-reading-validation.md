# AI 主题排版验收记录

日期：2026-09-07。交付范围为源码和本地功能；未提交、推送或发布安装包。

## 自动化验证

- 全量 `pnpm test:unit`：223 个文件、2,004 项测试通过。最终宽表格样式调整另通过 33 项内容、安全、可见性、桥接和标题相关测试。
- `pnpm typecheck`、`pnpm lint`、`pnpm build` 及 `git diff --check` 通过。
- 数据测试覆盖当前/上一/工作版本事务、取消和迟到结果、按检查点继续、图片原子落盘、任务分项用量、跨正文图片引用保护、完整备份恢复与旧库兼容。
- 内容与安全测试覆盖中文 Markdown、长文尾部、列表/表格/代码/链接保真、正文块一一对应、不可信 HTML/CSS/SVG、桥接消息、缺图读取与单文件导出上限。

相关命令输出保存在 `artifacts/themed-reading/{unit,typecheck,lint,build}-final.log`。早期失败取证与最终通过报告分别保留。

## 离屏视觉与交互

所有桌面验证均使用最新构建的 `pnpm shot`、独立临时数据目录和屏幕外窗口，没有使用 `--stale-ok`。

| 场景 | 结果与证据 |
| --- | --- |
| 首次生成面板、空页、执行中、部分失败、配图重试入口、过期提示、明暗/窄面板、处理中心 | 8 张状态截图，位于 `artifacts/themed-reading/shots-ui`；该组使用离线 IPC 夹具 |
| 真实啤酒主题页 | 12/12 正文块逐块匹配，3/3 图片加载；846px 与 366px 阅读区域无横向溢出；明暗主题、尾部查找、5 项目录、页内链接和图片放大通过；见 `shots-live/live-layout-report.json` |
| 技术教程、旅行、论坛总结、120 节长文 | 离线内容夹具；来源隔离、代码与表格、正文尾部和查找通过；长文 372 块，来源 Markdown 38,571 字符；见 `shots-content/content-evidence.json` |
| 窄面板的 8 列长表格 | 单元格最小 128px，表格在 286px 容器内横向滚动到 1,025px，最后一列可访问，366px 的整页不横向溢出 |
| 正式单文件 HTML | 在销毁宿主页面上下文后，以无权限沙盒和断网环境加载原导出字节；12 块正文与顺序相同，3 张内嵌图片成功解码，网络请求 0，脚本/本地资源地址/宿主接口均无；见 `shots-export/export-offline-evidence.json` |

视觉验收中发现并修复了编号加粗章节未进入目录、论坛标签在窄面板换行，以及长表格列宽被过度压缩的问题。图片放大和完整导出截图都等待图片解码及实际绘制完成后检查。

## 真实模型链路

输入为用户库中「生啤、熟啤、鲜啤、精酿的区别：三个独立维度与选购判断方法」。用户库只读，正文和图片复制到独立验收目录；没有更新用户文章，也没有复制模型凭证。

- 文本设计模型：`gpt-6-astra`。规划及布局成功，完整保留 12 个内容块。
- 生图模型：`gpt-image-2`。AI 计划新增 1 张图，上限为 3 张。
- 首轮生图在 240,089ms 命中原有 240 秒客户端超时；服务端是否完成该次请求未知。全文与两张原图仍成功发布为部分成功页。
- 新主题链路将单次等待上限设为 480 秒，正文配图原有默认值不变；没有隐式重发。仅手动追加 1 次补图请求，353,505ms 后完成。
- 两次尝试合计发出 2 次生图请求，最终新增 1 张 3,382,112 字节的图片；第二轮没有再次设计正文或处理成功原图。
- 最终页面含两张原图和一张主题插画，正式自包含导出为 10,190,777 字节。正文、设计和原图复用均经逐项核对。
- 两轮真实运行早于新任务分项统计代码加载，没有回填历史计数；分项统计以随后新增的自动化测试验证，真实请求次数由一次性预算文件和阶段事件记录证明。

完整证据位于 `artifacts/themed-reading/live-2026-09-07T07-11-46-747Z/acceptance-summary.json`。可直接打开的成品为同目录的 `reading-resume-2026-09-07T07-28-33-985Z.html`，SHA-256 为 `cc380b757701535d87acf070591a23554103d9c36720c303002b82f633d6f3c1`。

## 本轮边界

真实服务验证只针对上述已配置文本/生图模型与一篇文章；其他内容类型的截图使用离线夹具。没有进行第三次生图请求，没有安装包发版或第三方平台发布。旧备份兼容、恢复、删除和资源生命周期由隔离数据库/文件测试覆盖，未对用户正式数据库执行恢复或删除。

## 后续修复：主题页文字贴边

- 只读复现用户实际保存的啤酒页面：最外层 `padding:clamp(1.25rem,4vw,3.5rem)` 被安全清理移除，主题背景内侧没有留白。
- 由可信阅读样式为最外层正文容器提供水平 20–48px、垂直 16–36px 自适应内边距，不逐层叠加。重新打开的已有页面及新导出均使用该规则，无需重新设计或生图。
- 最新构建后运行 `pnpm shot --steps tests/e2e/themed-reading-spacing-shots.mjs`：1200px / 366px 阅读区域、明暗两种主题全部通过；实际左右内边距分别为 48px / 20px，12 个正文块逐块一致、图片完整、整页无横向溢出。证据在 `artifacts/themed-reading/shots-spacing/spacing-report.json`。
- 技术教程宽表格、旅行、论坛和超长正文的窄屏截图回归通过，见 `artifacts/themed-reading/shots-spacing-content`。35 项内容、安全、可见性、导出和标题测试，以及 typecheck、lint、build、差异检查通过。
- 本次没有调用模型或修改用户数据库；截图使用真实已保存布局的只读副本及隔离 IPC，未发布安装包。

## 后续修复：双滚动条与浮动进度

- 主题阅读区使用受约束的单一宿主滚动容器；iframe 不显示独立滚动条，加载完成后主动请求正文高度及目录布局，补偿首次观察通知与宿主订阅之间的时序风险。刷新消息仍校验父窗口和实例 ID。
- 目录按钮移入上方工具区；常驻正文上的「目录 · 百分比」浮层与迷你进度轨道移除，进度仅在用户展开目录后显示。阅读源变化时重置旧进度、选中章节与目录状态。
- 最新构建离屏检查：明暗与宽窄模式、iframe 内滚轮驱动宿主滚动且子文档 scrollY 为 0、开头 0% / 到底 100%、章节跳转全部通过。证据：`artifacts/themed-reading/shots-reader-polish`。
- 另一份真实保存布局的尾部查找、目录及图片放大通过，见 `shots-reader-live`；技术教程、旅行、论坛和长文使用离线内容夹具验证，见 `shots-reader-content`。没有新增模型调用。
- 32 项阅读组件、桥接、公众号快照、安全策略测试通过；新增主动刷新鉴权测试后桥接 3 项再通过。typecheck、lint、build 和差异检查通过。


## 后续修复：布局保留、目录交互与 HTML 表现力（2026-09-07）

- 从用户提供的鱼油、啤酒两份 HTML 只读重建回归版本；25 个正文块逐块同序、4 张图片摘要一致，原文件没有改动。
- 移除强制等宽的布局覆盖，保留经过安全清理的非对称网格和正文限宽。1280px 场景下鱼油目录从 543.5px 缩至 224px，正文从 543.5px 增至 768px；900px 场景目录位于正文上方，420px 无整页横向溢出。
- 目录补齐编号、链接提示、44px 点击区域、悬停和键盘焦点；鼠标、Tab、Enter、离线原生锚点和应用固定桥接跳转全部通过。
- 原文列表允许安全的 Grid 排版，列表项、引用、代码及表格单元格允许成对主题配色。额外分栏示例由测试代码构造，仅验证呈现能力，不是新模型生成结果。
- 修复百分比 row-gap 不计入 Grid 自动高度的问题。最终 420px 示例末卡底部与容器底部均为 2152.35px，下一节标题位于 2201.35px，保留 49px 间距；浅、深色卡片对比度分别为 10.22、10.65。
- 最终离屏验收覆盖 30 个状态、32 张截图；独立应用内容回归覆盖技术长表格、代码、旅行、论坛与长文末尾查找。104 项相关单测、typecheck、定向 ESLint、文件行数门禁和构建通过。
- 证据：`artifacts/themed-reading/user-regression-shots/user-html-evidence.json`、`artifacts/themed-reading/user-content-regression/content-evidence.json`；可打开的修复副本和呈现示例位于 `artifacts/themed-reading/user-regression/`。
- 本轮 0 次模型调用、0 次外网请求，截图使用隔离用户目录和只读导出副本；未修改用户数据库、未发布安装包。新的生成契约已更新，真实模型的新生成视觉质量仍需独立验收。

## 后续实施：内容驱动的专题设计（2026-09-07）

- 鱼油页恢复完整标题和导语主次，使用默认收起的目录、两段并列体验、品牌比较、判断条件及共识/分歧对照；去掉贯穿长文的空侧栏。
- 啤酒页突出完整主题标题，提供三个等权的概念导航入口，控制图文比例，强调第三维度的限定条件，并把结尾三个判断问题做成独立应用区。
- 将这些呈现能力实现为可选的 `reading-*` 组件并接入生成指导，允许模型按文章关系选用或自由设计；已有自由布局不自动注入组件样式。误放在辅助目录中的原文会强制展开。
- 两份成品由开发代码使用用户提供的 HTML 和原有图片重新编排，经正式清理、回填和文档组装路径输出；25 个原文块逐块同序，4 张图片的摘要和替代文字一致。桌面原文件与用户数据库没有修改。
- 107 项相关单测、类型检查、定向 ESLint、文件行数门禁和最新构建通过。离屏验收覆盖 1280 / 900 / 420px、深浅色的 12 个离线状态，以及 4 个应用桥接外观状态，检查整页溢出、正文对比度、列表边界和目录跳转。应用桥接测试故意设置与应用相反的系统外观，验证应用设置优先。
- 验收共 25 张截图、6 次离线/桥接键盘导航；主题文档网络请求为 0，导出脚本为 0，内嵌只执行固定桥接。首次验收误把宿主切页前的本地资源请求计入文档请求，已将监听起点移到隔离页面加载后；截图增加滚动后的重绘等待。
- 成品位于 `artifacts/themed-reading/editorial/{fish-oil,beer}-offline.html`；最终证据为 `shots-final/editorial-evidence.json`。此轮没有调用真实模型，没有更新库中已有设计，也没有提交或发版；新生成指导的真实模型表现仍需另行验收。

## 后续实施：以用户确认的重构稿为生成标准（2026-09-07）

- 新任务改为生成结构化专题：导读、导航、卡片、比较、图文、清单、矩阵、概念切换及两种算术工具，由可信渲染器输出 HTML。四种主题配色支持深浅色和窄屏。旧 HTML 页面与旧任务续跑保持兼容，禁止新旧章节混合。
- 摘录、归纳、推导均携带原文段落与摘句；主进程和数据库验证引用匹配、内容块覆盖、字段边界、素材声明。完整原文按原顺序保留并可展开核对。匹配只证明引用存在，不等于归纳语义或事实已经核实。
- 工具只执行固定的成本/总量公式或切换已生成的解释，不执行模型提供的 JavaScript。离线导出包含 CSP 哈希许可的固定脚本，应用内另加阅读桥接；两种模式均保留交互。
- 真实模型 `gpt-6-astra` 对两篇原始文章各完成一次设计：鱼油 156,535ms、啤酒 148,730ms，均为 6 个章节。共 2 次文本请求、0 次修复重试、0 次生图请求；25 个原文块完整保留，4 张原有图片复用。产物为 `artifacts/themed-reading/composition-live/{fish-oil,beer}-offline.html`，记录在同目录 `fixtures.json`。
- 受控组件夹具覆盖 12 个深浅色/宽窄状态及桥接外观、两种计算器和键盘概念切换；真实模型结果覆盖 8 个状态、24 张截图，正文、图片、工具及来源跳转通过。证据分别为 `composition/shots-final/composition-evidence.json` 和 `composition-live/shots/live-evidence.json`。
- 最新构建的真实阅读组件验收 8 张截图，验证导航驱动宿主滚动、引用展开原文、查找原文独有词；366px 内容区无横向溢出，子文档 scrollY 为 0。使用隔离测试条目与 IPC 注入已生成文档，证据为 `composition-live/app-shots/app-evidence.json`。
- 全量 2038 项单测、全仓类型检查、桌面全量 ESLint 和构建通过。最后合并重复引用编号后，13 项专题重构单测、桌面类型检查、定向 ESLint、构建及真实产物截图再次通过。全仓 lint 因本轮开始前已有的 `scripts/generate_fish_redesign.mjs` 为 1720 行、超过 1500 行门禁而失败；该脚本未改动。`git diff --check` 通过。
- 视觉比较和取舍见根目录 `design-qa.md`。没有改动桌面参考文件或用户正式数据库，没有提交、推送或发布安装包。库中旧页面需要执行调整设计或重新生成才会采用新设计。


## AI 重构阅读 v2 实施与验收（2026-09-07）

本轮采用新的产品约定：原文独立编辑；AI 页允许提炼、重组、扩写、研究和自由 HTML 设计。此前记录中的逐句引用、原文覆盖和完整原文附录仅适用于旧格式兼容，不再是 v2 的生成要求。

### 实施结果

- v2 保存原文快照、生成动作、完整理解笔记、研究查询与筛选检查点、取得的网页正文、编辑稿、修订基稿、自由设计和可信交互。仍使用原有当前／上一／工作版本及任务表。
- Tavily 官方搜索独立配置，固定查询和结果预算，密钥安全存储，连接测试、失败恢复与主动离线继续均已接入。查询成功、正文缺失、部分网页失败和资料不足分别处理，不将搜索摘要记为核实结果。
- 正文／讨论总结各自采用 AI 阅读、原文和编辑状态；有成品重新打开默认 AI，完成生成不抢占编辑或其他条目。原文与 AI 各自保存位置、目录；编辑失败保留稿件。公众号保留独立“网页快照”入口。
- 调整支持仅设计、内容与设计、最新原文重做。旧稿升级成功才替换。单图重试保留交互与稿件，备份恢复覆盖 v2，未知版本明确拒绝。
- 自由页面清理、固定运行时和 CSP 分开验证。支持计算器、情景、标签、筛选、导航和折叠；模型代码、网络资源与应用桥接不会进入离线导出。

### 自动化与构建

- `pnpm typecheck` 通过：`artifacts/reconstruction-typecheck.log`。
- 桌面全量 ESLint 通过：`artifacts/reconstruction-eslint.log`。
- 全量 **231 个文件、2070 项单测通过**：`artifacts/reconstruction-full-unit-final.log`。
- `pnpm build` 通过：`artifacts/reconstruction-build.log`；未打安装包、提交或推送。
- `git diff --check` 通过。
- 根目录 `pnpm lint` 被本轮开始前已有且未改动的 `scripts/generate_fish_redesign.mjs` 阻塞：1720 行，上限 1500。其余文件未报告行数超限。见 `artifacts/reconstruction-root-lint.log`。
- 新增测试覆盖算术解析与离线脚本一致性、恶意 HTML/CSS、伪造资料 ID、分段编辑与保存重试、初始查询乱序、完成生成时编辑／切条目、研究检查点、部分来源失败、离线继续、单图重试、v2 备份和流式传输完整性。旧格式、取消／恢复、资源生命周期与备份回归随全量测试通过。

### 真实文本模型产物

使用已配置的 `gpt-6-astra`，鱼油以讨论总结来源、啤酒以正文来源分别执行理解、提纲、写作与页面设计。不联网、不新增生图，未写入用户正式库。

最初两篇分别在整页设计阶段触发三分钟超时，编辑稿已保存。仅延长非流式超时后，两篇各两次设计请求仍连接失败。随后改为流式传输，从原编辑稿恢复，每篇一次设计请求成功并通过验证；没有重新撰写已完成章节。

总计 **27 次文本请求、0 次 Tavily 请求、0 次生图请求**。这包括上述失败请求。原始失败记录保留在 `reconstruction-live/fixtures.json`、`*-resumed.json`，成功记录及累计计数在 `reconstruction-live/accepted.json`，不把失败尝试抹去。

- 鱼油：`artifacts/themed-reading/reconstruction-live/fish-oil-offline.html`。海蓝与奶油色，重拟标题、判断结构、标签比较、SVG 与三个成本计算器。
- 啤酒：`artifacts/themed-reading/reconstruction-live/beer-offline.html`。琥珀专题，三个概念维度、工艺比较、阅读导航和三种选择场景。
- 原始 Markdown、模型配置、密钥与内部研究稿不附在导出的 HTML 中。没有外部资料的离线版本不虚构参考资料。

### 视觉和应用内验收

最新构建通过 `pnpm shot` 在隔离用户目录离屏执行，未使用 stale-ok。

- 两份实际模型输出覆盖 1440／420px、深浅色、首屏／正文中段／工具区，啤酒三个情景均经键盘切换。无整页横向溢出、脚本错误或外部资源请求。共 27 张截图，证据：`artifacts/themed-reading/reconstruction-live/shots-final/evidence.json`。
- 将实际生成结果注入隔离条目及正式阅读组件，验证默认 AI、原文切换、目录状态、滚动记忆、编辑保存和查找唯一新增文本。讨论总结保存后，主楼、回复、元数据仍存在。应用外观传播至 iframe，366px 阅读区无横向溢出，子文档 scrollY 为 0。共 12 张截图，证据：`artifacts/themed-reading/reconstruction-live/app-shots-final/evidence.json`。
- 独立 UI 夹具增加长原文目录和滚动，以及公众号网页快照入口／返回阅读，最终通过。普通创建接口不写采集来源表，因此快照用例只在隔离 IPC 的读取响应注入来源元数据；此用例不证明重新抓取网页成功。证据：`artifacts/themed-reading/reconstruction-fixture/app-shots-final/evidence.json`，14 张有效截图。
- 人工检查了鱼油首屏／暗色窄屏中段／工具区、啤酒首屏／暗色窄屏中段和应用内暗色：标题主次、正文宽度、边距、表格、图解和可点击导航符合参考方向。两篇布局由真实模型决定，没有用手写样例替代真实成品。

### 明确未完成的外部验收

用户确认暂时没有 Tavily 密钥，故**真实 Tavily 搜索、联网补充事实及真实联网参考资料未验收**。模拟网络测试与真实模型离线产物不能替代这一项。配置用户自己的密钥后，仍须执行两种来源的真实联网流程并保存实际查询、资料和请求计数。当前没有声称整套在线验收完成，也没有更改独立研究模块或把扩写写入 RAG／索引。
