# 更新日志 / Changelog

## v0.4.0

首个公开发布版本。在 v0.3.0-alpha.1（内部预发布）的基础上补齐了数据安全、
语义检索、媒体采集与 Wiki 图谱，并重构了知识库界面。

### 知识库界面重构

- 双视图：卡片视图（虚拟化列表 + 常驻详情面板，中间栏宽度可拖拽）与
  列表视图（全宽表格，详情在全屏弹窗打开），视图选择持久化
- 表格列配置：标题 / 摘要 / 标签 / 类型 / 知识库 / 状态 / 创建时间 /
  更新时间 / 操作可显隐、可拖拽调宽，复选框列与操作列左右吸附；
  每页 10/20/50/100 条分页
- 标签选择器改为浮层：当前标签、新建标签、已有标签与 AI 建议合并在一处，
  详情页与列表右键菜单共用
- 范围切换改为带计数徽标的分段控件，回收站独立置于侧栏底部
- 详情头重整：标题、收藏 / 置顶 / 归档 / 回收站操作与元信息 chip
  （类型、知识库、来源、保存状态、字数）一行收敛；正文与文字稿分 Tab
- 顶栏「新建」改为拆分按钮：主按钮开快速采集，下拉提供空白笔记与导入文件
- 修复：界面多处写死 `Ctrl+N` 作为快速采集提示，但 Windows 下不注册应用菜单，
  该组合键从未生效；改为读取主进程实际生效的快捷键（默认 `Alt+Shift+N`）

### 媒体采集（M9）

- 本地媒体文件导入：图片 / 音频 / 视频拖入快速采集即资产化入库
  （`local-image://`、`local-video://` 协议服务），详情页内联预览与播放
- 图片 OCR：详情页「识别图中文字」，走 visionText 路由的多模态模型，
  结果作为「图中文字」小节写入正文（进入全文检索与语义索引）
- 音视频转写：新增 audioText 模型路由（AI 工作台第五条路由，指向
  whisper 类模型）；详情页「生成文字稿」经主进程 multipart 上传
  `/audio/transcriptions`，文字稿写入 transcript（问答与语义索引使用）
- 在线视频采集：B 站 / YouTube / 抖音 / 小红书链接自动识别，yt-dlp
  解析元数据并下载音轨（免 ffmpeg），配置转写模型后自动生成文字稿
- yt-dlp 应用内管理：设置页一键安装（官方源 + 镜像回退、走网络代理、
  实时进度条），自动校验可执行性后托管在应用数据目录；支持更新 /
  移除内置版；状态卡显示来源（内置 / 系统 PATH / 自定义路径）与版本，
  自定义路径带文件选择器；未安装时导入降级保存链接并引导一键安装
- 修复：core 配置层路由白名单缺 embedding，导致语义检索的路由分配
  在重启后丢失

### Wiki 关系图谱（M10）

- Wiki 新增图谱视图：页面为节点（主题 / 实体 / 概念三色），
  物化的页间链接为边；缩放显示标题，点击节点跳转页面
- 图谱按需懒加载（react-force-graph-2d），不影响启动体积

### 语义检索（M8）

- Embedding 语义索引：条目分块（滑动窗口 + 标题前缀）→ embedding API 嵌入 →
  L2 归一化向量以 Float32 BLOB 落库（`knowledge_embeddings` 表）
- 增量索引：内容哈希 + 模型双重判定，编辑 / 换模型自动重新嵌入；
  后台每 5 分钟静默补索引，导入完成后延时触发；Ask 侧栏显示索引进度并可手动执行
- 混合检索：AI 问答的检索改为「FTS 关键词 + 语义向量」RRF 融合，
  同义改写、跨语言问法可召回；embedding 未配置或调用失败时静默退化为纯 FTS
- 模型接入：复用 AI 工作台已有的 embedding 路由（openai 协议 /v1/embeddings、
  Gemini OpenAI 兼容层；Anthropic 无 embeddings API 明确报不支持）

### 数据安全（M7）

- 本地备份：手动「立即备份」+ 定时自动备份（间隔 / 保留数可配），
  `VACUUM INTO` 在线一致性副本落在 `%userData%/backups`
- 从备份恢复：完整性与表结构校验、导入任务运行守卫、
  恢复前自动保存当前数据快照，完成后应用自动重启
- 升级前快照：应用内更新安装前自动备份一次（pre-update）
- Markdown 批量导出：frontmatter 元信息（标题/标签/集合/来源/时间），
  按知识库分文件夹，可直接导入 Obsidian 等工具；导出不携带任何机密

### 打磨（M7）

- AI 问答会话持久化：会话列表（切换 / 删除 / 新对话）落库 SQLite，
  重启后恢复上次会话
- 知识列表批量多选：Ctrl/Cmd+点击、Shift+范围选择，
  批量移动集合 / 归档 / 回收站 / 恢复 / 彻底删除，Esc 退出
- ask / wiki / imports 三模块侧栏面板补齐（会话历史、编译状态 + 最近页面、
  任务概览 + 快速采集），替换占位文案
- 「自动保存」开关真正生效：关闭后显示未保存标记，用保存按钮或 Ctrl+S 落盘
- macOS 菜单「导入 / 导出」接通（快速采集 / 数据设置）
- e2e 冒烟测试：真实 Electron 启动 → 建条目 → 全文搜索 → 手动备份
- 清理：移除无引用的 SettingsModal 与 isDatabaseEmpty 空壳、
  过时的里程碑注释与占位词条

## v0.3.0-alpha.1

内部预发布，未公开分发。归知从 .NET/Avalonia 全面迁移到 Electron/React 技术栈的首个版本。
UI 与交互框架源自 [PromptHub](https://github.com/legeling/PromptHub) v0.5.9（AGPL-3.0），
在其应用骨架上重建归知的全部知识管理能力。

### 应用骨架（M0）

- 保留 PromptHub 应用骨架：窗口/托盘/快捷键、Liquid Glass 主题
  （12 套莫兰迪主题色、深浅色、背景图）、设置框架、AI Provider 工作台、
  WebDAV/S3 传输、更新器与构建发布链
- 移除 PromptHub 业务域（Prompt/Skill/MCP/Plugin/Rules/Agents/Cloud/CLI）
- 品牌化：归知图标、appId `com.couleurshare.guizhi`、数据目录
  `%APPDATA%/GuiZhi`（新库 `data/knowledge.db`，与旧 .NET 版数据并存）
- 语言精简为简体中文 / English

### 知识库（M1）

- 知识条目：Markdown 编辑（CodeMirror）/ 预览、防抖自动保存、
  收藏 / 置顶 / 归档 / 回收站
- 知识库（集合）与标签管理（六色标签、自动建标签）
- 中文全文检索：FTS5 按字分词 + 英文前缀匹配，相关性排序
- 三栏工作区：范围导航（收件箱/全部/收藏/归档/回收站）+ 虚拟化列表 + 详情

### 采集管线（M2）

- 快速采集（Ctrl+N / 托盘）：粘贴文本或链接自动识别、.txt/.md 文件选择与拖放、
  指定目标知识库、空白笔记直达
- 导入队列：并发 2、持久化、重启恢复、取消 / 重试、进度角标
- 网页抓取：Readability 正文抽取 + Markdown 转换、SSRF 防护、
  编码回退、抓取失败自动降级保存链接
- 去重：规范化 URL（剥离跟踪参数）+ 内容哈希双重判定，
  重复条目可打开已有或强制创建副本

### AI 能力（M3）

- AI 问答：Agent 工具循环（search / read / answer 动作协议，检索即推理）
  优先，协议失败自动回退单发 RAG；回答带可跳转引用，来源按实际标注对齐
- AI 摘要：短文单发、长文 map-reduce 分块，写入条目摘要卡片
- AI 标签建议：一键生成可点选标签
- 多协议模型支持（OpenAI 兼容 / Gemini / Anthropic），
  场景级模型路由（摘要/标签走快速模型，问答/Wiki 走主力模型）

### 知识 Wiki（M4）

- AI 把知识条目增量编译为互链的 Wiki 页面网络（主题 / 实体 / 概念）
- `[[链接]]` 白名单清洗（幻觉链接降级纯文本）、反向链接、来源条目回溯
- 增量指纹（素材哈希 + 提示词版本），自动编译开关（默认关）+
  手动立即编译 / 全量重建
- 问答检索融合 Wiki：页面优先命中，阅读页面时出链与来源成为新的检索线索

### 数据迁移（M5）

- 首次启动自动检测旧版（.NET）`guizhi.db`，一键迁入条目 / 知识库 / 标签 /
  来源记录 / Wiki 全量数据（含编译指纹，不触发重编）
- 在临时拷贝上执行（原文件只读不动），WAL 日志自动降级兼容

### 已知限制

- macOS 安装包暂缓（待签名证书；图标资产待替换）
- 本地 OCR / 语音转写延后（当前依赖 OpenAI 兼容远程 API）
- 中文平台（B 站 / 小红书 / 抖音）视频解析待后续版本恢复

历史版本（.NET/Avalonia 时代 v0.1.x–v0.2.x）见旧仓库的 GitHub Releases。
