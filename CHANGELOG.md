# 更新日志 / Changelog

## v0.11.0

这版的主线是让归知里的东西能被外部 AI 直接用上。

场景是这样的：在抖音或 B 站刷到一条讲技术的视频，觉得对手上的项目有用，想把
链接发给 Cursor 或 Codex 问一句「这对我有没有用」。但直接发链接是问不出结果的
——抖音的详情接口对没有签名 cookie 的请求返回空，B 站视频的内容压根不在网页里、
在音轨里，对方只能从标题猜。而归知这边素材早就齐了：AI 拟的标题、结构化总结、
排过版的完整口播稿、图文的图中文字，缺的只是一个出口。

现在有两个出口。轻的那个是「复制给 AI」：条目详情页一颗按钮，把整条内容拼成一段
自包含的 Markdown，粘进任何 AI 对话框就能读。重的那个是 MCP：把归知接进 Cursor
或 Codex 之后，AI 自己去搜、自己去读，连复制都省了——而且归知不需要开着。

另一条主线是采集面。小红书的图文笔记与视频笔记现在能采了，分享口令整段粘进采集
框就行；侧栏多了「平台」分区，按抖音 / B 站 / 小红书 / YouTube / V2EX 分组。
换设备也不用再逐个重配模型和重贴 Key —— 全部设置能导出成一个文件带走。

单元测试从 1024 个增至 1214 个。

**行为变更（升级前请注意）**

- **本次升级会重算全库条目的「来源平台」。** 这一列建表时就在，但采集管线从来
  没写过，全库皆为空；唯一写过的是旧版 .NET 迁移，落进去的是老应用自己的一套
  取值。侧栏新增的「平台」分区要用它，所以这次是全部重算而不是只补空值——留着
  老取值会在分区里多出几个认不出来的分组。这一列此前没有任何读取方，重算不弄丢
  任何在用的数据。
- **AI 用量面板的数字会明显变大。** 此前只有问答、摘要、标签这三条链路在记账，
  主进程的配图、总结、排版、转写、OCR 一条都没记——而配图按张计费，是最贵的
  一项。现在补齐了，看到的才是真实开销，不是这版突然多花了钱。面板还多了排版 /
  语义索引 / 正文配图三栏。
- **同一个文件重复导入会被判为「重复」，不再各存一份。** 媒体资产改成按内容哈希
  命名，同一份文件产出的正文逐字相同，去重因此生效。确实想要两条的走「仍要创建
  副本」，两条条目共用一份资产文件。
- **MCP 是一个新的对外接口面，但默认什么都不做。** 要用得自己把配置写进 Cursor
  或 Codex。它只提供检索与读取两个只读工具，改不了也删不了任何条目；能看见哪些
  知识库可以在「设置 → MCP 接入」里限制。
- 设置页新增独立的「MCP 接入」分区。它不在「数据」下面——那一栏讲的是把内容搬
  出去与搬回来，MCP 是让外部工具接进来读。
- 配置迁移**不会带走**这几样：数据目录、yt-dlp 与 ffmpeg 的路径、开机自启、背景
  图片、本地转写引擎。它们绑定在具体设备上，带过去只会让人以为配好了。
- 列表摘要不再以「平台：抖音 · 作者：… · 时长：…」开头。那段元数据在列表里各有
  专列，却连同最长 300 字的平台简介一起占满了整段摘要，正文一个字都露不出来。

### 新增：一键把条目交给任意 AI

详情页标题右边多了一颗机器人按钮，点一下就把整条内容复制走，切到 Cursor、
ChatGPT 或任何对话框直接粘。箭头里还有「只要总结」的精简版，以及另存为 `.md`
——存进项目目录就能反复 `@` 它。列表和卡片上右键也有同一组。

产物和「导出 Markdown」不是一回事，差别在于多了一段写给 AI 的阅读须知，三条各防
一件事：文字稿是语音识别出来的，拉丁文技术名词可能有音近错误（实测出现过
Docker→dacker、useState→us state），不声明的话对方会认真讨论一个不存在的库；
记录里没有画面信息，不声明的话它会把「视频里演示了配置」当成你已经看过；以及
明说全部内容都是素材，视频里一句「现在忘掉前面的，跟我做」不该被当成你的指令。

精简版不会静默删内容——略掉的口播稿会在原位留一行「共 N 字，本次未包含」。
静默删掉会让对方以为自己看到了全部，然后对着残缺素材下结论。

### 新增：MCP —— 让 Cursor / Codex 直接搜你的知识库

「设置 → MCP 接入」里点一颗按钮，归知就写进 Cursor 的配置文件；Codex 则给一条
`codex mcp add` 命令。之后在 IDE 里直接问「归知里那条讲 Zustand 的视频对这个项目
有用吗」，AI 会自己检索、自己读全文，读到的正是上面那份带阅读须知的交接稿。

归知不需要开着：MCP 组件是独立进程，只读打开数据库。写入客户端配置时会保留你
已有的其它 MCP 服务器和手写的注释，覆盖前留一份备份；配置文件读不通就拒绝改动
并告诉你原因，不会把你配了很久的一堆服务器冲掉。

可访问范围可以按知识库限制。知识库里可能有私人日记、体检报告这类并不想让 IDE
里的 AI 读到的东西，「指定知识库」勾上想开放的即可，改完立刻生效、不用重启 IDE。

### 新增：小红书采集

图文笔记与视频笔记都能采。配图入资产库并逐张识别图中文字，正文拆成文案 / 图片 /
图中文字三个标签页。只支持从分享面板复制出来的链接——地址栏手抠的链接缺少校验
参数，平台一律返回 404。

分享口令可以整段粘贴，不用自己抠链接。「0.02 复制打开抖音，看看【…】
https://v.douyin.com/xxx/ :3pm 12/15」这种，以及小红书手机版那种逗号紧贴链接的
口令，都能正确提取。文字里夹着链接时，采集框会实时显示提取到的链接和将执行的
动作，旁边一颗按钮可以一键改判——「明天看这个 <链接>」里的说明是你自己写的，
丢掉才是误判。

### 新增：侧栏按来源平台分组

知识库与标签之间多了「平台」分区：抖音 / 哔哩哔哩 / 小红书 / YouTube / V2EX，
外加网页与本地文件两个兜底桶。只列有条目的平台，顺序固定，采集一条不会让侧栏
重排。表格视图也多了一列「来源」（默认显示）。

图标用的是各平台的真品牌 logo，不是形态近似的通用图标——此前抖音取音符、B 站
取电视，得先读懂旁边的文字才对得上号，而这一列的全部用处就是不看文字也能扫出
哪行是哪个平台。

### 新增：配置迁移，换设备不用重配

「设置 → 数据 → 配置迁移」把全部软件设置导出成一个 JSON：服务商与模型、模型
路由、配图风格、快捷键、外观与通用偏好。API Key 可以选择加密后一并带走，用一次性
的传输密码；忘了密码也只丢 Key，其余配置照常导入。

导入分两步：先看清文件里有什么（几个服务商、几个模型、带没带 Key）再决定替换。
替换前会把配置目录快照一份，导错了能拷回去。

### 新增：知识库图标从 11 个扩到 80 个

按「用户拿知识库装什么」分成八组：学习 / 工作 / 技术 / 创作 / 生活 / 财务 /
旅行 / 标记。只收老码位的表情——新 emoji 在 Windows 10 上是豆腐块，而这一格的
全部用处就是一眼认出来。

### 改进：AI 用量记账覆盖主进程

配图、总结、排版、转写、OCR 现在都会记账。记账绝不连累主流程：拿不到数据库句柄
就静默跳过。生图按「一张图」记而不是按「一次请求」记——重试只发生在服务端错误
与限流上，那些请求没出图也不计费，按尝试次数记会凭空放大账单；内置本地转写引擎
不记，它跑在你自己的 CPU 上。

### 修复

- 气泡提示不再「冒出来又跳一下」，靠近屏幕底部时翻到元素上方而不是压住它。
- 列表摘要剥掉开头的元数据引用块，正文终于露得出来。
- 采集弹窗里现敲的标签会立刻出现在侧栏，不用刷新。
- AI 用量的「失败次数」此前恒为 0——统计链路上漏了一环，现在接上了。

## v0.10.0

这版的主线是「正文配图」：让 AI 读一遍长文，挑出几处值得画的地方，逐张生成手绘
风格的插图并插进正文对应段落之后。长文难读往往不是因为字多，而是一段抽象的论述
没有落点——配一张图，那段话就有了可以回看的锚。

另一条主线是长音视频那条链路。转写 → 排版 → 总结三步加起来动辄十几分钟，此前
界面上只有一个转圈的按钮，「在跑」和「卡死」长得一模一样；排版的单块超时又偏紧，
被自己掐掉的请求会让整篇作废，而失败原因只进了控制台。现在全程报「当前阶段 +
已用时长」，排版改成并发分块、中途失败只丢没排完的那部分，后台失败一律落进日志。

顺带补上说话人分离：访谈、会议的文字稿现在可以带「说话人 1：」这样的对话体前缀。

单元测试从 853 个增至 1024 个。

**行为变更（升级前请注意）**

- **新增第六条模型路由「正文配图」（imageGen）。** 要用配图得先在「设置 → 模型
  服务」挂一个文生图模型（gpt-image-2、gemini-3-pro-image 之类），它是专用路由，
  不参与对话。**生图按张计费**，每张约 30~60 秒；能力卡片上的「测试当前配置」
  会真实画一张最小尺寸的图，这一次同样计费，不是空跑。
- 配图目前只适配 OpenAI 的 `/images/generations` 与 Gemini 的原生接口。Anthropic
  没有文生图 API，配上去会给出明确提示而不是撞 404。
- 文字稿 AI 排版的单块超时从 120 秒放宽到 240 秒，并新增按块数摊算的总时间预算。
  实测同一条提示词在中转站上耗时 75 / 117 / 130 秒都出现过，120 秒正好卡在正常
  区间的上沿，于是「本来会成功」的请求被自己的超时掐掉。
- 排版中途失败不再整篇作废：已排好的部分保留，剩下的按原文接回去，并明说「只排了
  一部分」。但第一块就失败仍然直接报错——一块都没成多半是模型配错了，降级成
  「部分成功」会把真问题盖住。
- 「设置 → 采集」新增「导入时区分说话人」开关，**默认关**。它只有内置本地引擎
  支持，会让转写慢一倍，单人内容开着没有收益。
- 内置本地转写引擎的线程数从固定 4 改为按机器核数取一半（下限 4、上限 12）。
  实测 5 分钟音频从 14.6 秒降到 12.6 秒——收益有限，所以刻意不占满机器，
  转写要跑好几分钟，期间整台机器不该卡。
- 「设置 → 数据」多了一颗「打开日志」。后台任务（定时备份、后台 Wiki 编译、
  配图生成）失败不弹窗，但从此都会记进 `error.log`。写进日志的路径会抹掉用户
  主目录，可以放心发出来求助。

### 新增：正文配图——AI 读完正文，在关键处画一张插图

分两步，中间停下来让你过目。第一步「策划」走文本模型读一遍正文，输出一份配图
规格：每张图放在第几段之后、图题是什么、画面里出现哪些物件、图上标哪几个词。
这一步不生成图片，也几乎不花钱。第二步按你勾选的规格逐张生成。

规格里的「画面里的物件」要求取自原文，并且做成可编辑的——放任图像模型自己编
例子，画出来的东西会和正文自相矛盾。实测一篇讲「四象限理性消费」的文章，正文把
手机归在「买好的」，图里却把手机画进了「谨慎购买」那一格。花钱之前扫一眼这一栏，
这类错就拦下来了。

张数可以指定，也可以交给「自动」（按可配图的段落数推导，约每 3 段一张）。生成是
串行的：按张计费、每张几十秒，串行才有稳定进度、可以中途停止，也不容易撞上模型
服务商的每分钟图片数限额。中途停止会把已经画好的照常写进正文——钱已经花了。

五张里挂一张是常事：中转站在一池上游渠道之间按成功率轮询，各渠道的内容安全严格
程度并不一致，同一份提示词换个渠道就过。所以失败那张会连同原因留在面板里单独
补生成，不必重新策划整篇；服务端 5xx 与限流自动退避重试两次，确认是被内容安全
拦下时会去掉排除项重画一次（被拦下的请求不出图也不计费）。

配图以图片文件写进正文，导出 Markdown 与全文检索都认得。「全部清空」会连图片
文件一并回收，删了找不回来，所以要过一次确认。

### 新增：配图风格可以自己改，也能分享给别人

内置六套，刻意在「底色 / 媒介 / 有没有人」三个维度上分开：手绘笔记（白底线稿，
方法与观点类）、淡墨速写（灰阶墨线，随笔与概念类）、深色蓝图（深靛蓝底，系统与
链路）、暖调生活（暖色块，健康与消费类）、双色小人（唯一带固定角色，靠姿态与
距离说事）、等距场景（轻等距视角，设备摆位与连接）。选择器里每套下面都有一行
「适合什么内容」——光看名字，选不出该用哪套。

风格是数据不是代码：画法与配色、固定角色、画幅、单图标注上限都能在应用内编辑
（配图面板里的「编辑风格」，或「设置 → 正文配图」，两处是同一个编辑器），改完
保存即生效，不用重启。单套风格能导出成一段 JSON 复制走，别人粘进来就能用。风格
选择按知识库记住，同一个库里的东西不用一篇篇重选。

策划那一步顺带会推荐一套它觉得更配这篇的风格——模型本来就在读正文，零额外调用。
只做提示，不自动替你切换。

### 新增：说话人分离

访谈、会议这类多人内容，转写结果现在可以带「说话人 1：」「说话人 2：」的对话体
前缀，相邻同一说话人合并成一段。两个入口：条目详情页的「重新生成并区分说话人」，
以及「设置 → 采集」里的导入时开关。界面只在当前转写模型支持时才显示这个入口。

前缀能活过 AI 排版：不明确要求保留的话，模型会把它当成口播赘语删掉，分离就白做
了——所以排完会数一遍，数量对不上判不合格重试。

已知边界：材料太少会塌成一个人（实测 15 秒 3 轮的对话全判成一个人，28 秒 5 轮
完全正确），所以只识别出一个说话人时给的是黄色提醒而不是绿色的「已生成」——它
既可能是真的单人，也可能是没分出来，得你自己判断。换人发生在同一句话中间（对方
接话没有明显停顿）时，整段会被判给同一个人。

### 改进：长音视频全程看得见进度

转写是一个不返回中间结果的长请求，20 分钟的访谈在 CPU 上要跑好几分钟。现在按秒
报「正在转写…已用 3 分 12 秒」，本地引擎每处理完一段还会更新一次心跳，超过一分钟
没有进展会明说「但已有 1 分 5 秒没有进展」。

计时覆盖整条链路并带上当前阶段：转写完还有排版和总结，这两步同样以分钟计。此前
只给转写计时，后两步期间界面会停在最后一个数字上、文案还写着「正在转写」，看起来
和卡死一模一样。刻意不报百分比——本地引擎给不出可用的分母，写死的分母只会骗人。

详情页的「AI 排版」按钮现在按块报进度。自动排版的字数上限是 5 万字（约 3 小时
口播），但超过这个数的稿子恰恰最需要分段，所以按钮允许你确认后越过：确认框里会
写明字数和预估的请求次数。

### 改进：本地转写不再写坏专有名词

实测内置引擎纯中文逐字正确，一旦中英混杂，错误全部落在拉丁词上：Docker 听成
dacker、useState 听成 us state、TypeScript 听成 typepescript。而专名恰恰是全文
检索与语义召回最依赖的那批词。

现在会把条目标题与简介里的拉丁词抓成一份术语表，塞进本来就要发的那次排版调用，
等于在文本层补上热词，不额外花钱。指令收紧到「只把文中已有的错误形式改成表里的
写法，表以外的词不要改动」——放开成自由改写就是拿幻觉换错字：A/B 实测里，不带表
的模型把听错的「cicopowerformer」改成了一个像模像样、但根本不存在的
「CICOPowerFormer」。编出来的错名比留着乱码更糟，它看着像真的。

排版本身也改成并发分块（3 路）：块之间互不依赖，而中转站单次要几十秒，串行会让
排版成为仅次于转写的一段耗时。

### 修复：那些「失败了却不说话」的地方

这版系统性地清了一遍。失败提示现在保持一句话，完整报错折叠在「查看详情」里，
可展开、可复制去搜；批量操作说清「N 成功 M 失败」，逐条原因进详情。

- 快捷键保存失败会回滚并说明原因。此前主进程返回的失败被忽略，界面照样弹一句
  绿色的「已保存」，而按键其实没注册上。
- 图文条目的逐张 OCR：9 张里 8 张失败，此前看到的还是绿色的「完成」。
- 备份列表读不出来会被渲染成「还没有备份」，可能让人以为定时备份没在跑。
- 自动备份失败现在带得出原因（磁盘满还是路径没权限）。此前只有一句「详见日志」，
  而那个日志当时还没有出口。
- 更新检查失败的原因一直存着却从没显示过，「关于」页只写「失败」两个字。
- Wiki 编译「10 条编了 3 条」，剩下 7 条为什么没成从来没被收集过——是被
  max_tokens 截断，还是输出压根不是 JSON，处置完全不同。
- 条目列表、Wiki 目录与 Wiki 搜索加载失败时不再渲染成空态。「读不出来」和「真的
  没有」在界面上长得一样，看到「暂无条目」没人会想到去重试。Wiki 搜索此前显示成
  「没有匹配的页面」，是最误导的一处。
- 改标签、改状态、移到回收站、清空回收站这一批操作，失败时界面此前毫无反应，
  用户以为改成功了；现在统一走一条错误通道弹出来。

## v0.9.0

这版没有新增采集来源，主线是把问答、Wiki、导入这三个模块的界面从「能跑通」补到
「能用」。它们此前都停在最小可用的状态：Wiki 只有一列翻不完的目录，搜索框只能匹配
标题；导入队列是一长列长得一模一样的 URL，看不出哪个在跑、跑到哪一步；问答回答里的
`[1]` 是纯文字，点不动。

另一条主线是本地语音转写。自带的 FunASR 引擎此前吐出的是一整段没有标点的文字，
几千字连成一片没法读——现在改用归知自己的转写服务脚本，标点与数字规范化都打开了，
升级即生效，不必重装引擎。

顺带把界面里最后几处由操作系统绘制的原生控件换掉：悬停提示、取色器、下拉菜单，
以及删除模型时那个系统确认框。

单元测试从 730 个增至 853 个。

**行为变更（升级前请注意）**

- **两条新的数据库迁移，启动时自动执行。其中一条会重建条目表**，把「收件箱 /
  已整理 / 已归档」三种状态压成「活跃 / 已归档」两种。这是一次全表拷贝，正文越多
  越久；合并之后无法还原。**建议升级前先在「设置 → 数据」做一次备份。** 另一条
  给导入任务加一列，瞬间完成。
- 数据结构版本从 v6 升到 v8。**新版产生的备份无法恢复进旧版应用**（会被明确拒绝
  并提示先升级），旧备份恢复进新版没问题。
- 「收件箱」范围改名「未分类」，含义也变了：现在指「还没归入任何知识库、且没有
  归档」的条目，归好类它自己就清零。原来那个 inbox 状态要手动点「移入知识库」才
  会消失，而它不 gate 任何东西——问答检索、Wiki 编译、语义索引从来都不看它，
  于是所有条目永远停在收件箱，和「全部」显示的是同一批。右键菜单里的「移入知识库」
  相应移除。
- 侧栏知识库与标签的条目计数改为排除归档，与点进去看到的列表口径一致——此前会
  出现侧栏显示 10、点进去只有 7。
- 本地转写的结果开始带标点。已入库的文字稿不会回溯重写，重新转写才会变。
- Wiki 侧栏从「编译状态卡片 + 最近更新列表」改成筛选轴，编译入口与状态统一收进
  工作区顶栏；删除页面从与「编辑」并排改为收进「更多」菜单。
- 导入列表的行内操作按钮改为悬停或键盘聚焦时才浮出，不再常驻。
- 问答里删掉一轮问答后如果会话空了，会话本身也会被删除；「重新回答」会连带丢弃
  该轮之后的所有问答（它们的上下文依赖被重答的这一轮）。
- 数字输入框不再显示上下步进箭头，键盘方向键仍按步长生效。箭头由浏览器绘制，
  颜色和几何尺寸都改不了，留着就是主题里唯一一块跟不上配色的地方。

### 新增：Wiki 搜得到正文，孤立页第一次有了入口

Wiki 页面的全文索引在 v0.8 就建好了，但只有 AI 问答在用——界面上那个搜索框做的
仍是标题子串匹配。现在它走同一套索引，搜得到正文，按相关度排序。目录支持按最近
更新、被引用最多、标题三种排序，列宽可以拖动并记住。

侧栏原来是 `catalog.slice(0, 15)`，和中间那列同源同序，等于把同一批标题在屏幕上
列了两遍。现在改成筛选轴：按类型（主题 / 实体 / 概念）分，以及「需要留意」下的
孤立页与手动编辑过的页面，各带计数。孤立页是没有任何页面链向它的页面——在一个
靠互链组织的知识网络里，这批恰恰最该被看见，而此前没有任何入口能找到它们。

详情页把反向链接从正文末尾提到了正文之前（「谁引用了这一页」是导航信息，不是脚注），
长页面新增可折叠的页内目录。顶栏的编译按钮现在直接显示待编译条数，没有新内容时
置灰——此前点下去只会弹一句「没有需要编译的条目」。

顺带修掉一个从 M4 起就在的排版毛病：详情页头部已经渲染了页面标题，模型又常在正文
首行放一个同名的一级标题，同一句话在页面上叠两遍。新编译的页面在落库前剥掉，已入库
的老页面在渲染时兜底——提示词管不住模型排版，但这层清洗是确定性的。故意没有升编译
提示词的版本号：那个常量参与编译指纹，升一下等于让全库页面重编一遍，为纯排版问题
烧一整轮 AI 调用不划算。

### 新增：本地语音转写终于有标点了

FunASR 自带的 `funasr-server` 调 `AutoModel` 时不传 `use_itn` 与
`sentence_timestamp`，HTTP 端点也没有透传这两个参数的口子。后果是 SenseVoice 走
无 ITN 分支，输出一整段不带标点的文字，而时间轴退化成按字符数均分总时长的合成值，
对不上语音。

现在改用归知自己的转写服务脚本，把这两个开关打开：标点与逆文本归一化（「二零二五年」
→「2025 年」）跟着出来，句级时间轴取自 VAD 的真实切分。两者都不额外耗时。脚本随
应用一起分发、启动前落盘，所以已经装了引擎的用户升级后自动拿到新版，不必重装。

入库前还会清掉 VAD 分段拼接留下的痕迹：中日韩文字与全角标点之间的多余空格、
接缝处叠出来的「，。」。这层清洗对云端转写结果同样生效，且是幂等的。

### 新增：导入队列看得出每个任务在干什么

一列长得一模一样的 URL 现在变成：采集途中拿到真实标题就换上，配上条目类型图标、
来源站点、文件任务的所在目录，以及已用时长。转写这类动辄几十分钟的任务，单个阶段
卡住超过 90 秒会补一条「本阶段已 X」——此前只能盯着一个不动的状态徽章猜它是在跑
还是死了。刻意没做「第 N 步 / 共 M 步」：实际步数取决于运行时条件，写死的分母只会
骗人。

侧栏那几行状态计数原本长得像导航却点不动，现在是真的筛选器（全部 / 进行中 / 已完成 /
重复 / 失败 / 已取消），页头补了搜索框，标题和链接都能搜。支持 Shift 范围多选与批量
重试、取消、删除，批量操作按选中项的实际状态分派；失败任务多的时候可以一键重试全部。

缺 yt-dlp 或 ffmpeg 导致的失败，现在直接给一个「前往设置安装工具」的按钮——这类
失败重试多少次都好不了。队列为空时的引导页说清楚归知能采什么，并给出快速采集与
导入文件两个入口。

### 新增：问答的引用可点，长回答不再拽着页面跑

回答里的 `[1]`、`【2】`、`[1,2]` 现在是可点的标记，点一下高亮出对应的来源卡片，
再点卡片跳回原文。行内代码与围栏代码块里的方括号不会被误伤，没有对应来源的编号
原样保留、不做成死链。

流式输出期间原本每来一个字就把滚动条拉到底，想往回翻根本翻不动。现在只有本来就停在
底部时才自动跟随，翻上去之后出现一个「回到最新」的按钮。检索过程在运行中只显示当前
这一步，不再把已完成的步骤全堆着让卡片不停变长、正文往下跳。

每轮问答悬停浮出复制、重新回答、删除三个动作，不必为一个问歪的问题清空整个会话；
回答上标出用的哪个模型，本轮没走通 Agent 检索协议时标一枚「简易检索」徽章。空对话
页给了三条可点的示例问法，未配置 embedding 模型时明确说明「当前只做关键词检索」
并直达设置。

### 整改：界面里最后几处系统原生控件

悬停提示此前是操作系统绘制的浅色小方块，约一秒才出现，字体和配色与应用完全两回事，
靠近窗口边缘还会被裁掉。现在由一个全局图层接管：悬停时摘走原生 `title` 自绘气泡、
移开再装回，320 毫秒出现，跟随主题配色，下方放不下会翻到上方，键盘 Tab 到按钮同样
能看到——这是原生 `title` 做不到的。

同时清掉了四十来处「气泡把按钮上已经写着的字再念一遍」的无效提示，划过侧栏和表格
不会再一路弹窗。判据写进了项目约定：纯图标控件该写（气泡是它唯一的名字），气泡内容
与可见文字不同的该写（绝对时间对相对时间、URL 对显示名、快捷键），把同一句话重复
一遍的不写。新增的 eslint 规则 `guizhi/no-redundant-title` 会挡住回潮，另外两条规则
禁掉了原生 `<select>`、`<input type="color">` 与 `alert/confirm/prompt`。

- 自定义主题色改用应用内取色器（取色板 + 色相条 + 十六进制输入，方向键可微调），
  不再跳出系统取色对话框。
- 分页的「每页条数」换成自绘下拉。顺带修好了向上展开时的定位：原先按估算高度反推
  位置，选项撑不满时面板会悬在半空。
- AI 设置里删除模型的 `confirm()` 换成应用内确认弹窗——这是全仓库最后一个系统弹窗。
- 滑块、数字步进箭头、搜索框清除叉这些浏览器自绘的默认外观统一接上主题令牌。
- 外观设置的主题模式从分段控件改成三块磁贴，与下面的字号、动效变成同一套语言。

### 修复：语义索引按钮「点了没反应」

侧栏「索引 N 条新内容」数的是 SQL 预筛出来的候选数，而候选里混着一批正文其实没变的
条目——收藏、打标签、写 AI 摘要都会推高更新时间，却都不进嵌入文本。于是按钮上长期
挂着一个数字，点下去待办是空的，界面上就是「点了没反应」。现在这个数字与真正会跑的
批次同口径，确认没变的条目会被踢出候选，下一次判定更便宜。

手动触发的索引跑完会给回执：成功多少条、部分失败带失败原因、全部失败带错误消息
（比如 embedding 接口 401）、没有内容可索引。此前这条链路从头到尾只往控制台写。
后台自动索引仍然静默。

### 修复：几处「看着像卡住」的等待

- 冷启动后第一次打开设置页，「采集」那三行要一起转圈约 3 秒。yt-dlp 是 PyInstaller
  单文件包，每跑一次 `--version` 都要解压整个 Python 运行时（本机实测 2.8 秒），
  而原有的缓存只活在进程内。现在按「路径 + 大小 + 修改时间」把版本号缓存到磁盘，
  文件没换就直接复用；点「重新检测」仍会真去探测。
- 首次切换到问答 / Wiki / 导入会闪一下加载转圈，现在这些模块的资源在启动后的空闲
  时段就预取好了。
- 加载指示器延迟 250 毫秒才淡入。本地数据库走一趟通常几十毫秒就回来，立刻画一个
  转圈再抹掉，这一下闪现本身就是用户看到的闪烁。
- 问答 / Wiki / 导入在数据读出来之前不再先铺一屏空态引导再被真实内容整块换掉；
  读取失败时也会放行到空态，而不是永远停在加载中。
- 详情页改分类不再等防抖，改为立即落盘并刷新列表与侧栏计数。

## v0.8.0

这版有两条主线。一是修掉编辑器上一个会真正丢数据的缺陷：连续切换条目时，上一条
没写完的改动会被写到下一条身上；同时补上了退出前的落盘兜底，改完不按 Ctrl+S
直接关窗，内容也不再消失。二是把大知识库的卡顿系统性地量了一遍并修掉——两万条
规模下语义检索从 15.1 秒降到 1.4 秒，列表首屏从 281 毫秒降到 16 毫秒。

功能侧新增三块：问答改成流式输出（长回答不再空等十几秒）、Wiki 页面可以手动
编辑与删除且不会被下一轮编译覆盖、条目列表支持键盘操作与批量打标签。

单元测试从 599 个增至 730 个。

**行为变更（升级前请注意）**

- 三条新的数据库迁移，都是加列，启动时自动执行：Wiki 页面的手动编辑标记、
  采集任务携带的标签、AI 用量的失败次数。另有两张新索引（条目全文索引的
  rowid 映射、Wiki 页面全文索引），首次启动会为已有数据回填，库大时会多花几秒。
- 删除条目不再弹确认框，改为直接删除并给出一个可撤销的提示。回收站里的彻底
  删除与清空回收站仍然要确认。
- 条目列表接管了键盘：方向键移动、Enter 打开、Home/End 跳首尾、空格多选、
  Delete 删除。焦点在输入框、编辑器或按钮上时一律不接管。
- 导出的 Markdown 会把正文引用到的图片与音视频一并拷进 `assets/` 子目录，
  导出体积因此接近这些资产本身的大小；换来的是导出结果在别的编辑器里能正常
  显示，此前那些链接指向应用内部路径，拿到外面全是破图。
- 启动时的数据库完整性检查改为按需执行：上次正常退出且距上次检查不满一周就
  跳过。崩溃、断电后的下一次启动照常全量校验。
- yt-dlp 与 FunASR 的下载新增 SHA-256 校验。官方校验和取不到时降级放行并记
  日志，取到但对不上会直接换下一个镜像。

### 修复：编辑会串到别的条目上，退出前未保存的改动会丢

保存队列原本全局只留一份待写补丁。在一条目还没写完时切到下一条，前一条的改动
会带着新条目的 id 落盘——用户看到的是「我明明改的是 A，内容却出现在 B 里」。
现在按条目分桶排队，每条各自合并各自的补丁；收藏与置顶的乐观更新同样按条目
对账，快速连续切换时标签不会回跳。

退出路径此前没有兜底：autoSave 有 800 毫秒防抖，在这个窗口里关掉窗口，最后
一次改动就没了。现在关闭时先取消这一次关闭，等待落盘完成后再关；主进程的退出
也改走窗口关闭流程而不是直接退出应用——否则数据库会先一步关掉，渲染进程发出的
写入无处可去。e2e 用例覆盖了真实的关窗与重启：改完标题不按 Ctrl+S 直接关窗，
重开应用标题仍在。

### 性能：两万条规模下的三处长卡顿

新增的压测脚本按真实数据量（2 万条、364MB）逐项测量每个操作占用主进程多久。
主进程被占住时整个界面是不响应的，所以这些数字就是用户感知到的卡顿时长。

- **语义检索 15106ms → 1448ms。** 分批读取向量原本用 `LIMIT/OFFSET` 翻页，
  而 OFFSET 要求 SQLite 先扫过并丢弃前 N 行，遍历全表于是成了平方级复杂度——
  120 个批次累计丢弃约 360 万行。改成按 rowid 游标翻页后，最后一批与第一批
  耗时持平。
- **列表首屏 281ms → 16ms。** 一半靠新增的 `(deleted_at, status)` 覆盖索引，
  让总数统计全程在索引里跑完，不必为读一个 status 字段回表——条目表每行带着
  约 2KB 正文，两万次回表就是两万次大页读。另一半靠启动时的 ANALYZE：没有
  统计信息时查询规划器会把 `deleted_at IS NULL` 当成高选择度的等值约束，
  挑中它之后还要对全部匹配行建临时 B 树排序。
- **启动完整性检查 920ms → 0ms。** 改成只在上次没能正常退出后才跑，正常退出
  会留下标记文件。标记超过一周仍强制查一次——磁盘坏块不挑应用退没退干净。

此外，列表查询不再把整篇正文搬过进程边界（改为按摘要长度截取），全文索引加了
rowid 映射，消掉写路径上的全索引扫描——自动保存每 800 毫秒触发一次，这条路径
上的全表扫描代价会随库增长不断放大。

`docs/sqlite-main-process.md` 记下了「要不要把 SQLite 移出主进程」的结论：不移。
优化后日常操作全部在 20 毫秒以内，剩下两处长阻塞各有比重构架构便宜得多的解法。

### 新增：问答改成流式输出

回答此前要等模型整段生成完才显示，长回答下界面十几秒没有任何动静，看着像卡死。
现在增量解析模型输出，逐字显示。问答内部是一个多轮的检索—阅读—回答循环，
只有最后那轮的正文会送到界面上，中间的检索与阅读动作不会漏出来。

检索侧同时修了两处会导致「明明有相关内容却答不上来」的错位：

- 自然语言问句原本被编译成整段相邻匹配，几乎必然零命中。新增按词召回的检索
  模式。
- 模型决定「读某条目」时，原本总是从条目开头截一段给它。命中位置在文档中后部
  时，读到的完全是另一段内容。现在把全文索引的匹配偏移传下去，按命中位置取
  阅读窗口。
- Wiki 页面接入全文索引后才真正可检索。此前靠内存子串计分，对中文基本失效——
  只有问句里逐字包含页面标题才加得上分，编译出来的页面网络在问答侧几乎不可达。

AI 用量统计补上了失败调用。超时与限流同样会产生费用，只记成功次数会让用量看着
比实际少。

### 新增：Wiki 页面可手动编辑与删除

此前 Wiki 页面完全由 AI 生成，用户改不了也删不掉——即使改了，下一轮编译也会把
模型的输出盖回去。现在页面支持手动编辑与删除，手改过的页面会被标记，后续编译
只更新它的链接关系、不动正文；不想再手工维护时，详情页可以「交回自动编译」
把标记清掉。

顺带修了三处：编译回写时如果没有替换正文，原有链接会被一并清空；编译进度把
从未真正生成过的占位页也算进了已完成数，显示偏高；关系图谱在页面较多时全量
渲染卡顿，改成按连接度截断并在界面上注明当前显示了多少个节点。

### 新增：条目列表的键盘操作与批量打标签

卡片与表格两个视图共用一套键盘导航。此前整个知识库里只有两个输入框和一个拖拽
手柄响应键盘，收件箱里几十条待整理只能一路点鼠标。

删除改成先删再给一个可撤销的提示，不再每次都弹确认框打断操作。提示层顺带修了
两处：带操作按钮的提示不会被后来的提示挤掉，持久提示优先于普通提示保留——此前
错误提示经常还没看清就被下一条覆盖。

批量操作补上了打标签与收藏；快速采集弹窗可以直接输标签、Ctrl+Enter 提交；
顶栏搜索框按当前模块显隐，不在知识库时不再抢 Tab 焦点。

### 修复：导出的 Markdown 是一堆破图，恢复备份可能截断主库

导出此前只写正文，图片和音视频的链接仍指向应用内部的自定义协议与资产库绝对
路径，导出目录拿到别处打开就是满屏破图。现在把引用到的资产拷进 `assets/`
子目录并把链接改写成相对路径，导出结果可以直接交给 Obsidian、Typora 这类
编辑器。知识库目录名顺带处理了 Windows 保留设备名与大小写冲突——撞上这两种
情况时，此前整次导出会中断在半路。

恢复备份原本直接覆盖主库文件，拷贝中途失败（磁盘写满、杀毒软件占用）会把主库
截成半个文件。而恢复入口就在应用里，主库一坏用户没有任何自救路径。现在改成
先拷到同目录的临时文件再原子改名，失败时用恢复前刚存的快照回滚。

备份保留策略之前漏掉了更新前、恢复前这两类自动快照，且排序依据是文件系统时间
（拷贝、迁移都会改写它）；现在改从文件名解析时间戳，三类快照各自计数。自动
备份期间主进程会冻住一到两秒，现在开始与失败都会发通知，不再无声卡住界面。

### 修复：外部工具下载缺少校验，SSRF 判定漏掉 IPv6 写法

yt-dlp 与 FunASR 运行时此前只验证「下载完能不能跑起来」，这拦不住被替换的
可执行文件。现在先取官方的 SHA-256 校验和逐个比对，对不上就换下一个镜像。
取不到校验和时降级放行并记日志——正需要镜像的网络环境里，官方源多半也是不通的，
在这里硬失败等于直接封死这个功能。ffmpeg 上游不提供校验和，已在代码里注明。

网络请求的内网防护漏掉了 IPv4-compatible 的 IPv6 写法（`::127.0.0.1` 这类），
能绕过内网地址拦截。同时把 `::` 与 `::1` 从内嵌 IPv4 的解码里排除，避免误判；
代理软件用的 fake-IP 网段现在只在确实配置了代理时才放行。

采集时选定的标签会随任务一起持久化，重启恢复后仍能打到入库的条目上；失败的
任务可以单条删除，不必清空整个列表。

### 工程：ESLint 此前一条规则都没生效

ESLint 的配置没有引入任何预设，规则集实际是空的——`--max-warnings 0` 看着严格，
其实一条问题也检不出来。现在显式启用了 JS 基础规则、TypeScript 推荐规则，以及
需要类型信息的浮空 Promise 检查，并一并修掉了规则启用后暴露出的存量问题：漏掉
的 Promise 处理、未使用的变量、写反的转义、React 副作用缺依赖。

同时新增了中英文案的键位对称性测试，任一边缺键都会导致测试失败。此前靠人工比对，
实际漏过若干只有中文的键，英文界面上直接显示的是键名本身。

## v0.7.1

v0.7.0 的修复版，三条都是「用起来会卡住」那一类：Wiki 编译经常在生成中途被掐断、
编译开始后没有任何办法停下来、以及一次渲染异常就能让整个窗口变成一块纯色。

单元测试从 590 个增至 599 个。

### 修复：Wiki 编译经常中途失败

主进程给 AI 请求留了一道 30 秒的兜底超时，这个数字当初是按「连通性探测」定的，
却被所有走它的调用一并套上。Wiki 编译是全应用最重的一次模型调用——一条条目正文
最多 3000 字、目录 30 行，再带最多 5 个上下文页（每页正文上限 8000 字），还要生成
2048 token 的 JSON，30 秒往往不够，请求就被掐断在生成半路。

知识域的模型调用现在统一放宽到 120 秒，Wiki 单条目编译进一步放宽到 180 秒。
代价是一条卡住的条目最长要占 3 分钟才判失败，所以配套补上了下面的停止按钮。

### 新增：Wiki 编译可以随时停止

编译进行中，工具栏上会出现停止按钮。此前没有这个入口，点了「立即编译」就只能
等整个队列跑完。

停止会真正中断在途的模型请求，而不是等当前这条跑完再停——取消信号原先只在条目
与条目的间隙生效，而单条编译现在最长可以跑 3 分钟，那样的「停止」等于没有。
已经编译落库的页面全部保留，本轮没轮到的条目留到下一轮继续，不会重复消耗调用。

### 修复：界面偶发变成一整块纯色

知识库列表按条目类型取图标和名称时用的是直接下标取值。条目类型来自数据库，
可能是当前版本还不认识的取值——比如数据库被更高版本写入过（装过新版本后又回退），
里面就存着旧版本没见过的类型。取值拿到 undefined，再读它的字段就抛异常，
React 随即卸载整棵组件树，窗口上只剩 index.html 的防白闪底色。用户看到一整块
纯色，除了控制台没有任何线索。

修了两层：条目类型与状态改成带兜底的查表函数，认不出来的值显示成「未知类型」
「未知状态」而不是崩溃；根节点加了错误边界，万一还有别的渲染异常，页面上会显示
错误原因和一个「重新加载」按钮。错误边界的文案刻意不走 i18n——i18n 自身出问题时，
这一层仍然必须显示得出来。同理，i18n 初始化失败现在也不再阻断界面挂载。

### Wiki 工具栏

- 「自动编译」从原生复选框换成开关控件。深色玻璃工具栏上的原生 checkbox 是个
  突兀的白方块，和左右两侧的控件也不在同一套视觉语言里。
- 视图切换与编译操作之间补了一道分隔线分组。
- 编译进度里的条目标题过长时截断，此前会把整条工具栏挤变形。

## v0.7.0

新增论坛帖子采集：V2EX 的讨论帖现在能完整入库，并由 AI 把几十上百条回复
按方案归类成一份可读的总结。另一半是采集引擎设置的整改——yt-dlp、ffmpeg、
本地转写三块拆散在三个设置页，这版合并成一个「采集」区，顺带补上了引擎的
版本检查与更新检查链路的可诊断性。

单元测试从 475 个增至 590 个。

**行为变更（升级前请注意）**

- 新增 `forum` 条目类型，数据库随之有一条重建 `knowledge_items` 的迁移。
  迁移会在启动时自动执行，执行期间临时关闭外键强制并在提交前校验，
  标签关联、来源记录、Wiki 关联与语义向量都不受影响。回滚到旧版本会因为
  `item_type` 的 CHECK 约束读不了论坛条目，降级前请先备份。
- V2EX 链接不再走通用网页抓取。升级前采集过的 V2EX 条目仍是 `webpage`
  类型且保持原样，重新采集会因为来源地址相同被判为重复。
- 采集设置的三个独立入口（yt-dlp / ffmpeg / 本地转写）合并为「采集」区，
  原来的三个设置页已删除。已保存的自定义路径不受影响。

### 新增：论坛帖子采集（V2EX）

论坛帖的价值常常不在主楼而在讨论区，而此前粘贴 V2EX 链接走的是通用网页抓取，
拿到的东西并不能用。以「外面访问家里局域网最优雅的方式是？」这个 107 条回复的
帖子实测：Readability 只抽到 72 条回复（最早的 1~5 楼全丢），正文里 37% 的字符
是头像图片地址这类噪音，节点、作者、发帖时间一概没有。

V2EX 因此改走官方的 v1 只读接口（`/api/topics/show.json` +
`/api/replies/show.json`）：无需 token，限额 600 次/小时，一次采集用两次，
回复接口不分页、一次返回整帖。同一个帖子现在能拿到全部 107 条回复、零噪音，
外加节点与发帖时间。

- 条目类型是新增的 `forum`，正文结构为「元数据引用块 + 讨论总结 + 正文 +
  逐楼讨论」，详情页据此拆成三个标签页，打开默认落在讨论总结上——主楼往往
  只是提问，结论都在回复里。
- 讨论总结不复用视频总结那套提示词。口播稿是单人按时间线叙述，顺着讲一遍
  就是好总结；论坛帖是几十上百人各说各的，同一个方案散落在十几个楼层里，
  还夹着灌水与玩梗。这里要求按「方案 / 观点」归类、统计各方案有多少人支持、
  保留实测数据与踩坑经验，并单独交代共识与分歧。走 mainText 路由，超长帖
  按回复边界分块做 map-reduce（上限 8 块）。
- 未配置文本模型或总结失败都不阻断采集，原始讨论照常完整入库，正文里如实
  注明原因。详情页可以随时点「重新生成讨论总结」补上：素材取自库中正文已存
  的逐楼回复，不重新抓网页——原帖可能已被删或又多了几十楼，条目自己那份
  才与你看到的内容一致。
- 带 `#reply107` 锚点的链接与裸链接会归一到同一个来源地址，不会各存一份。

只支持 V2EX。Discourse 那套 `/t/{id}.json` 在 linux.do 这类站点会被
Cloudflare 挡成 403，没有能跨站通用的开放协议，只能逐站适配，认不出来的
论坛链接一律退回通用网页抓取。

### 修复：AI 总结里的小标题和正文黏成一段

讨论总结最初让模型把小标题写成独占一行的 `**加粗**`，页面上却渲染成
「**基于 IPv6 的 DDNS 直连** 多人推荐此方案，认为……」——标题和正文挤在
同一段里，完全失去了分隔作用。原因是 Markdown 的单换行会被折叠，
`**标题**\n正文` 属于同一个段落。

小标题因此统一改用 `###`：标题是块级元素，不依赖空行就能独立成块，还带上了
字号层级与段间距。提示词管不住模型排版，所以另加了一层确定性清洗，把模型
吐出的 `#`、`##` 与独占一行的 `**加粗**` 一律规范成 `###`，列表项里的加粗
（`- **ZeroTier**：延迟 4ms`）不受影响。`##` 还必须拉平——它会和 `## 正文`、
`## 讨论` 这两个分段锚点撞车，把详情页的标签页切错。

同一个帖子改造前后对比：小标题从 0 个变成 8 个，列表条目 29 条，
超过 80 字的整段从多段降到 1 段（开头概述），字数反而从 3096 降到 2006。

### 采集引擎设置合并为「采集」区

yt-dlp、ffmpeg、本地转写引擎原本各占一个设置页，三页的信息密度都很低。
现在合并成一个「采集」区，每个引擎一行，主行只留「名称 + 状态 + 主操作」，
路径设置、重新检测、移除都收进各行的高级面板。

- 引擎状态探测加了 5 分钟 TTL 缓存并合并并发探测。这几项探测都很贵：
  yt-dlp 与 ffmpeg 要 spawn 可执行文件跑 `--version`（各带 10 秒超时，
  回退查 PATH 时会跑两次），本地转写要对 127.0.0.1 发健康检查（3 秒超时，
  未启动时必然等满）。此前每次进设置页都会重新跑一遍，React StrictMode 的
  双次挂载还会撞成两份。安装 / 移除后由调用方显式失效，点「重新检测」
  可强制绕过缓存。
- yt-dlp 与 ffmpeg 支持检查更新。两者的可比标识不同：yt-dlp 用 release tag，
  ffmpeg 上游是滚动 tag、改用资产的构建日期。查询失败时如实报「检查失败」，
  不会误报「已是最新」。

### 更新检查：把无声的失败暴露出来

自动检查更新在界面上是完全无声的——没有新版本和检查失败长得一模一样，
用户只能得出「自动检查没生效」的结论，事后也无从排查。

- 每次检查的结果都写进 `startup.log`：触发来源（手动 / 启动 / 周期 /
  窗口重新可见）、渠道、耗时、结果、版本号或错误。
- 自动检查的跳过判定从 `App.tsx` 的 useEffect 里抽成纯函数，五种跳过原因
  （未启用、窗口不可见、离线、已有检查在途、冷却中）各自可测，跳过时同样
  留下日志。

## v0.6.0

一轮围绕「抖音内容采集」的功能发布。抖音链接此前必然失败，本版把它从 yt-dlp
的死路上摘了出来，并顺着这条链路把图文作品补成完整能力：配图入库、自动 OCR、
图片查看器、正文分区。期间还捞出两个影响面更广的渲染缺陷（Markdown 列表无
序号、正文里的本地图片是破图）。

单元测试从 423 个增至 475 个。

**行为变更（升级前请注意）**

- 抖音条目的来源链接统一收敛为 `douyin.com/video/{id}` 或 `douyin.com/note/{id}`。
  升级前采集的旧条目不会自动改写，同一视频再采一次可能判定为新条目。
- 图文作品的条目类型是 image 而非 note，详情页的正文拆成了三个标签页。
- 图文采集会在配了 visionText 路由时逐图调用视觉模型（默认上限 9 张），
  按张计费。不想产生这笔开销就清空该路由。

### 修复：抖音链接一律采集失败

抖音链接进导入队列必然失败，错误是 yt-dlp 抛的
`Fresh cookies (not necessarily logged in) are needed`。

原因在 yt-dlp 侧：它的 Douyin 提取器打的是
`douyin.com/aweme/v1/web/aweme/detail/`，这个接口对没有签名 cookie
（`__ac_signature` / `ttwid`，由页面 JS 挑战当场生成）的请求返回 200 加一个
空 body，提取器解不出 JSON 就报了上面那句。给 yt-dlp 传浏览器 cookie 也基本
无解——Chrome 127 起的 App-Bound Encryption 让 `--cookies-from-browser`
在 Windows 上对 Chrome / Edge 直接失效。

抖音因此不再经过 yt-dlp。新的 `import/douyin.ts` 走 `iesdouyin.com` 的分享页：
在移动端 UA 下这个页面是服务端渲染的，作品信息全在 `window._ROUTER_DATA` 里，
播放地址把 `playwm` 换成 `play` 就是无水印源，全程不需要任何 cookie。标题、
作者、时长、文案照常入库，配了语音转写模型的话音轨也照常下载转写。桌面 UA 会
被 302 回 `douyin.com` 走上那条需要 cookie 的老路，所以 UA 是这条链路的前提。

顺带的行为变化：

- 短链（`v.douyin.com/xxx`）、分享链、站内链、用户主页上点开的 `modal_id`
  形态，现在都会收敛到 `douyin.com/video/{id}` 这一个来源地址，同一个视频换
  几种链接粘贴不会再各存一份。
- 图文作品的配图会下载进本地资产库并嵌入正文，配了视觉模型（visionText 路由）
  时采集当场逐图 OCR，识别结果作为「图中文字」小节入库，图里的表格与流程图
  因此能进全文检索与语义索引。图文的信息主体常常就在图里，只留文案等于丢了
  内容；而抖音图片地址带 `x-expires` 签名（约一个月），不落盘过期就再也取不
  回来。逐图识别按张计费，默认只识别前 9 张，其余图片照常入库并在正文注明；
  未配置视觉模型或识别失败也会如实写进正文，不静默留白。条目类型为 image，
  详情页的「识别图中文字」可随时重跑，现在也支持多图。
- 抖音会给图文同时生成一个幻灯片视频，那个既不下载也不转写——判定图文只看
  `images` 字段，不能看有没有播放地址。
- 图文文案是纯文本，入库前会转成等价的 Markdown（逐行分段、转义行首的 `1.`
  `-` `#`），否则渲染时单换行会被并成一段、序号会被列表语义吞掉、列表后面
  的段落会被吸进最后一项。
- 「重新生成文字稿」对抖音条目同样改走这条路，不再要求装 yt-dlp。
- 采集失败时透出抖音自己给的原因（如「该作品已被作者删除」），而不是一串
  yt-dlp 的英文栈。

B 站 / YouTube / 小红书不受影响，仍由 yt-dlp 解析。

### 改进：图片条目的详情页

- 图文条目的标题改由 AI 拟定（素材是文案 + 图中文字）。抖音图文没有标题字段，
  此前取文案首行，往往是半句话。拟题失败或未配文本模型时退回原来的首行。
- 长标题不再被单行输入框截断，标题随内容增高，最多三行后内部滚动。
- 图片可点击放大：全屏查看器基于 `yet-another-react-lightbox`，支持滚轮缩放、
  双击放大、拖拽平移、键盘 +/-/方向键、多图切换与触控板手势。正文里内嵌的
  图片同样可点开。
- 图片条目的正文拆成「文案 / 图片 / 图中文字」三个标签分开看，三者仍存在同一
  份 Markdown 里（全文检索与语义索引不受影响）；点「显示原文」回到完整源码。
- 顶部不再单独放一份图片预览——「图片」标签页里已经有了，重复占版面。
- 图片条目不再显示「文字稿」标签页——它没有音轨。此前图文条目的来源链接
  是抖音，被误判成可转写的在线视频。
- 「识别图中文字」「生成 AI 摘要」从两个裸文字链接改成与周围元信息一致的
  chip 按钮，并排成一行。

### 修复：正文里的本地图片渲染成破图

`![](local-image://…)` 在渲染视图里始终是一个空 `<img>`。这条路上有两道闸门：
`rehype-sanitize` 的协议白名单只放行 http/https，react-markdown 自带的
`urlTransform` 也会把非 http/https 的地址替换成空串。两处都放行后本地资产图
才会显示。受影响的是所有在正文里引用本地图片的条目。

### 修复：Markdown 列表的序号与圆点一直没显示

条目正文、AI 问答回答、AI 摘要卡片、Wiki 页面——凡是渲染 Markdown 的地方，
列表都只有缩进，看不到 `1. 2. 3.` 和圆点。Tailwind 的 preflight 会把 `ul` /
`ol` 的 `list-style` 清成 `none`，而 `globals.css` 里手写的 `.prose` 规则只设了
缩进和间距，从没把它加回来。有序列表因此会直接丢失序号这一层信息。

现在按浏览器默认恢复：无序列表 disc / circle / square 三级交替，有序列表
decimal。GFM 任务列表自带复选框，单独去掉记号，避免圆点和复选框并排。

## v0.5.0

一轮以「把看起来做完、实际会丢东西的功能补成真的」为主线的集中整改。
三条主线：数据正确性（搜索排序、保存失败、导入失败上报）、安全边界
（渲染进程运行时边界、IPC 读写白名单、机密存储）、规模与可控性
（服务端分页、AI 请求可中断、Wiki 内容不再被覆盖）。

单元测试从 265 个增至 423 个，新增端到端安全用例。

**行为变更（升级前请注意）**

- yt-dlp / ffmpeg 的自定义路径输入框改为只读，只接受文件选择器选中的文件。
  已保存的路径继续生效，需要更换时点旁边的按钮重选。
- 设置项按字段白名单读写，历史遗留的未知键（含已下线的 WebDAV / S3 配置）
  会在下次启动时被清理。
- 导入任务的「清理已完成」不再删除失败任务——那里保存着原始输入与失败原因，
  是重试的唯一入口。

### 修复：删除条目不会清理磁盘上的媒体文件

导入图片/音视频时文件会被拷进资产目录，但删除条目只删数据库行。导入一个
1GB 的视频、彻底删除条目、清空回收站，那 1GB 会永久留在 `%APPDATA%/GuiZhi`
里，用户无从得知。

现在彻底删除与清空回收站会连带回收资产：删除前先收集正文里的引用（那时正文
还在），删除后逐个检查是否仍被其他条目引用，确认无人引用才删文件。资产文件名
来自用户可编辑的正文，因此走白名单校验——`local-image://..` 拼出来正是资产
目录的父目录。

### 修复：视频导入全程只显示「抓取中」

视频链路会串起元数据解析、音轨下载、转码、转写、排版、总结六步，最长可达
几十分钟，而此前它们全部落在一个 `fetching` 状态里。用户完全无法判断是在
正常推进还是已经卡死。现在每一步都会上报，任务列表实时显示当前子阶段。

### 修复：OCR 与转写在非 OpenAI 协议下必然失败

OCR 的请求体与响应解析都硬编码了 OpenAI 形态。Anthropic 的 `/v1/messages`
不认 `image_url`（图片必须拆成 base64 的 source 块），响应也是顶层 `content`
而非 `choices[0].message` —— 配了 Claude 做视觉模型的用户，一次 400、一次
「响应缺少文本内容」。现在按协议分支，多模态转换与文本提取都下沉到共享的
`ai-protocol`，与对话链路共用一份实现。

转写则是另一种情况：Anthropic 根本没有转写 API，Gemini 的音频要走
`generateContent` 内联，都不存在 `/v1/audio/transcriptions`。此前一律拼这个
路径，用户只会撞上一个莫名的 404。现在协议不支持时直接给出可读提示，并指向
内置转写引擎。

### 安全：AI 端点访问收敛

`ai:httpRequest` 天然要访问用户配置的任意地址，无法像网页抓取那样禁私网，
残留的风险是「注入后用这条通道静默扫内网」——响应体会完整回传。

现在已保存在配置里的 host 与回环地址（本地 Ollama / LM Studio）不受限，
其余 host 按每 10 秒一个限速：用户手点测试连接完全不受影响，而扫描需要成百
上千次尝试，被限速后不再可行。这是缓解不是根治——彻底的解法是把连接测试与
模型列表拉取搬进主进程，让这条通道只服务已保存的端点。

### 重构：拆分 ai.ts

`ai.ts` 一个文件里塞着传输抽象、错误格式化、SSE 解析、provider 适配、
请求体构建、参数兼容重试、模型列表六件事。拆成四个模块后主文件从 1006 行
降到 503 行：

- `ai-transport.ts`：IPC / fetch 双路径与取消绑定
- `ai-errors.ts`：结构化错误、HTML 错误页、网关超时的可读化
- `ai-stream.ts`：SSE 解析与增量分发
- `ai-models.ts`：四种 `/models` 响应形态的归一

顺带删掉三处死代码（`isGeminiApiHost`、`isGeminiOpenAICompatEndpoint`，以及
`getErrorMessageFromResponse` 这个零参数包装）。`ai.ts` 仍是公开入口，
拆出去的符号继续从这里透出，调用方无需改动。

### 修复：批量粘贴链接会被存成一条文本笔记

采集框只要输入含任何空白就判定为非链接。从浏览器书签或笔记里复制一批链接
粘进来，整段会被塞进一条文本笔记——没有报错也没有提示，用户以为导入了 20
个网页，实际得到一条纯文本。这是采集最常用的动作之一。

现在按空白切分，全部是 http(s) 链接时逐个入队（批内自动去重），输入框下方
实时显示「识别到 N 个链接」。混进说明文字时仍整体按文本保存，不丢上下文。
顺带补上入队失败的错误提示——此前 IPC 抛错会让弹窗僵在原地，用户只能反复点。

### 性能：语义检索不再卡住整个界面

余弦 top-k 是全量扫描，此前一次性把整份索引搬过 wasm 边界再连着算完，
全程占着主进程。上万条目时单次检索要搬几百 MB 并做数千万次乘加，期间所有
IPC 排队——卡住的不只是问答，而是整个界面。

改为分批取用（每批 500 块）并在批间让出事件循环。内存峰值从整份索引降到
几 MB，界面在检索期间保持可交互。总耗时不变——真正的解法是 ANN 索引，
那是后续的事。

### 新增：AI 用量统计

`usage` 字段在类型里声明了却从没被读过。一次提问最坏会触发十来次模型调用，
Wiki 后台编译也在持续消耗，而界面上没有任何地方能看出用了多少。

现在知识域的每次调用都会记账（按 天 × 场景 × 模型 聚合，保留 90 天），
AI 设置页底部展示最近 30 天的调用次数与 token 分布。provider 不回报 token
时只统计次数，并在界面上说明。

### 基础设施：schema 迁移执行器

`schema_migrations` 表建库以来一直是空的——`init.ts` 里那段「迁移」只做了建表，
没有注册表、没有执行循环、没有一条读写记录，而 `schema.ts` 的注释却写着
「列级变更需走 schema_migrations（见 init.ts）」。

后果是整个 schema 只能加新表：给既有表加一列时 `CREATE TABLE IF NOT EXISTS`
会整条跳过，新列只对全新安装生效，老用户升级后一读新列就 `no such column`。
这次补上真正的执行器：迁移逐条独立事务、按名去重、写 `PRAGMA user_version`
版本戳；列级变更统一走 `addColumnIfMissing` 之类的幂等守卫，新库老库都安全。

备份校验随之能做版本比对了：来自更新版本的备份会被明确拒绝并提示先升级应用，
而不是恢复完再撞上读不懂的结构。

### 修复：Wiki 编译会静默覆盖页面内容

编译一条新知识时，模型能看到全部页面的**目录**（标题 + 摘要），但只有 top-3
相关页附了正文、且截断到 800 字。提示词却要求它「输出合并新旧知识后的完整
正文」，落库又是整体覆盖。于是最坏情况是：某页在目录里但不在上下文里，模型
照着标题现编一段新正文，**原页全文被覆盖且无从找回**；即便在上下文里，模型
看到的也只是截断后的半页，长期反复「截断—重写」会持续丢历史信息。

三处一起改：

- 附完整正文的页面从 3 提到 5，正文上限从 800 提到 2000（显著大于 body 的
  目标长度，否则「合并」必然丢内容）
- 落库侧只信任本轮真正附过正文的页面 id，其余既有页**保留原 body**，只更新
  摘要与别名，等下一轮它进了上下文再重写
- 提示词同步收紧，明确告知模型哪些页可以更新、哪些只能引用

同时新增 `wiki_page_revisions`：每次覆盖前存一份快照（每页保留 10 份），
页面详情多了「恢复上一版」入口，内容被写坏时可以直接回滚。

### 修复：Wiki 编译失败会永久重试

模型始终解析不出来的条目不会写指纹，于是每 5 分钟的后台编译都会重新捡起它，
每次消耗两次模型调用，且永不停止——一条「毒条目」可以无限期烧钱。现在失败
会记录失败次数与下次重试时间，退避 30 分钟起、每次 ×4，连续 3 次后不再自动
重试，直到素材变化或手动全量重建。

### 修复：知识库超过 200 条后，多出来的条目无法访问

分页一直在客户端做：服务端固定返回前 200 条，界面再对这 200 条切片。库里有
更多条目时，第 201 条之后**没有任何入口可以打开**——不是加载慢，是不存在。
同一屏还会自相矛盾，工具条按服务端总数显示「共 5000 个」，页脚按已加载条数
显示「共 200 个」。

分页改到服务端：`page` / `pageSize` 进 store，直接落到 SQL 的 LIMIT/OFFSET。
卡片视图与列表视图共用同一套分页状态，两边看到的数据范围始终一致；分页条抽成
`ItemPagination` 由两个视图复用。删光当前页（例如清理最后一页）时自动回退到
最后一个有内容的页。

顺带修掉一个并发竞态：`fetchList` 此前没有请求序号，快速切换范围时先发出的慢
请求后到会覆盖新结果，列表内容与侧栏高亮对不上，`isLoading` 也会被提前收起。
现在只有最后一次发出的请求可以写回。

### 修复：AI 的「停止」按钮此前不会真的停止

`ai:httpStreamCancel` 频道在常量表里定义了，但全仓没有任何 handler 或调用方；
`ChatCompletionOptions` 与 `runScenarioChat` 都不接受 signal，问答里的
`throwIfAborted` 只在调用前后查一下标志位。结果是点了停止之后，按钮要等那次
LLM 请求自然返回才复位，在途的 HTTP 请求照常跑完。

而它可能永远不返回：主进程的超时计时器在 fetch resolve（**响应头到达**）时就被
清掉了，后面的 `response.text()` 与流读取没有任何超时保护。provider 发完 200 头
就卡住（中转站限流排队很常见）时，请求会永久挂起，且停不掉。

这次把链路补齐：频道改名 `ai:httpCancel` 并注册 handler，主进程按 requestId
维护在途请求表；渲染进程的 `AbortSignal` 经 requestId 转达过去，取消后统一按
`AbortError` 抛出。超时计时器改为响应体读完才撤销，流式与非流式都覆盖。
问答的两条管线（Agent 循环与单发兜底）都会把 signal 透传到每一次对话调用，
并有单测盯住这条链路不再断开。

### 安全：补上渲染进程的运行时边界

归知会把导入的第三方网页转成 Markdown 再渲染，正文是不可信内容，而此前
渲染层的 `rehype-sanitize` 是唯一一道防线。新增 `main/window-security.ts`：

- **CSP**：生产策略不含 `unsafe-inline` / `unsafe-eval`，即使 sanitize 被
  绕过，注入的脚本也不会执行；本地媒体协议与正文外链图片保持可渲染
- **导航拦截**：preload 的 `window.api` 绑在 window 上、不区分来源，一旦
  主窗口被导航走，对方就拿到完整 IPC 能力。现在只允许停留在渲染产物目录
  与 dev server 同源，外链转交系统浏览器；`file://` 也限制在产物目录内，
  磁盘上任意 HTML（含导入到资产目录的文件）都不可达
- **权限**：Electron 默认放行多数 web 权限请求，本地知识库一个都不需要，
  一律拒绝

新增 e2e 用例在真实 Electron 里验证前两条确实生效。

### 安全：settings 表的读写边界

`settings:get` 是整表回传、`settings:set` 是遍历入参对象写入，两个方向都
没有边界。表里同时装着主进程内部状态，于是主密码记录会随设置一起进入渲染
进程；反过来渲染进程也能往表里写任意键——包括 `ytDlpPath`，而它会被主进程
`spawn`，等于把一次渲染层脚本注入直接升格成主进程任意代码执行。

现在两个方向都过 `Settings` 字段白名单，并且 `ytDlpPath` / `ffmpegPath`
只接受本次会话中由文件选择器返回的路径（清空始终允许）。对应的两个设置项
输入框改为只读，旁边补了清除按钮。

### 安全：AI 通道与网页抓取

`ai:httpRequest` 此前把渲染进程给的 URL 原样交给 fetch，响应体与全部响应头
完整回传，是一个不设防的 HTTP 代理。它和网页抓取不同——本地 Ollama、局域网
推理服务都是常规用法，不能套用「一律禁私网」，所以挡的是没有任何合法 AI
用途的目标：非 http(s) 协议，以及 link-local（含云元数据 169.254.169.254）、
组播、保留段；域名同样解析后校验，避免用指向元数据地址的域名绕过。

网页抓取补上 IP 钉扎：此前校验完 DNS 就丢弃解析结果、仍按主机名连接，
存在「校验后 DNS 再变」的 rebinding 窗口。现在连接钉在刚校验过的地址上，
`servername` 保 SNI、`Host` 头保虚拟主机。走代理时不钉扎——目标域名由代理
解析，钉扎反而会绕过按域名分流的规则。

顺带修好一个可用性问题：Clash 等代理的 fake-ip 池落在 198.18/15，该段被判
为私网，开着系统代理的用户网页与图片导入会全部失败。`net-safety` 早就为此
准备了 `allowProxyCompatibilityAddress`，但三个调用方一个都没传。

### 安全：主密码不再做代码不支持的承诺

设置页写着「未解锁时私密内容不可见」，但 `encryptText` / `decryptText` /
`getKey` 全仓零调用方——没有任何内容被加密或隐藏过。这是向用户做出的、
代码不支持的安全承诺，文案已改为如实说明当前状态。

存储格式同时修正：原先把 scrypt 派生出的密钥原样写进 settings 表，既当校验
值又当 AES 密钥，等于密钥和密文躺在同一个文件里。现在只存密钥的 sha256，
密钥仅在解锁期间留在内存；已有记录在下次成功解锁时就地升级，用户无感。

### 安全：清理 WebDAV / S3 遗留字段

v0.4.1 删掉同步功能后，老用户 localStorage 里仍留着那批字段——合并是无过滤
展开、`partialize` 又原样写回，脏键的生命周期等于 localStorage 的生命周期。
其中 `webdavPassword` / `s3SecretAccessKey` 是明文凭据，而界面上已经看不到
这些配置项，用户无从知晓、更无从删除。现在读写两个方向都过字段白名单，
未知字段在下次启动时自然消失。

### 修复：全文检索的三处失准

**标题命中排在正文命中之后。** `bm25()` 的列权重按列序位置映射，虚表第一列
`item_id UNINDEXED` 同样占一个位置，而调用只传了三个权重，导致整体错开一列——
本意的「标题 10 / 正文 1 / 标签 5」实际生效成「标题 1 / 正文 5 / 标签 1」，
正文里顺带提一句的条目会排在标题精确命中之前。补上占位后恢复预期。

**带标点的查询必定无结果。** 查询串里的纯标点片段（`(`、`：`、`——`）经
unicode61 分词后是空 phrase，空 phrase 匹配 0 行，再与其他子句 AND 就把整个
查询清零。搜「归知(测试)」「第一章：概述」都返回空。现在这类片段被丢弃。

**回收站里搜不到任何东西。** 软删会把条目移出 FTS 索引，但回收站列表仍然
JOIN 索引，想找回一条误删的笔记只会看到「回收站是空的」。改为软删保留索引
（列表查询自带 `deleted_at` 过滤，不会污染常规检索），仅在彻底删除时移除。
老库里已缺失的索引行在启动时自动补齐。恢复条目的写路径顺带从每条 5 次查询
降到 1 次。

### 修复：抓取失败被伪装成导入成功

网页抓取失败、yt-dlp 未安装时，连接器会返回一个只含链接的占位条目并标记
`degraded`——但这个标记从来没有消费方。任务照常标记为 completed（界面显示
绿色勾），知识库里多出一条空壳，而空壳的 `sourceUri` 还会占住该链接的
`normalized_uri`，导致此后重新导入永远判重为「重复」，指向那个空壳。
yt-dlp 的安装指引又让用户「回列表点重试」，而重试按钮只对失败任务显示。

现在降级结果不再入库：任务标记为 failed 并透出具体原因，重试按钮正常出现，
链接也不会被占住。「清理已完成」相应地不再连带删除失败任务——那里保存着
原始输入与失败原因，是唯一的重试入口。

### 修复：编辑保存失败会静默丢改动

`flushPendingSave` 在发起 IPC 之前就清空了待保存内容并把「未保存」标志置为
false，请求失败时只打一行 console.error。数据库被备份流程占用、条目已被删除
等情况下，用户的编辑就这样消失了，而详情页头部仍然显示「更新于 xx」。
现在失败会把改动退回待保存队列（await 期间的新输入优先），头部显示红色的
「保存失败，改动未丢失」并保留手动保存按钮。

### 修复：重启后最早入队的导入任务永远不再执行

崩溃恢复用的是「最近 200 条」的默认列表查询。一次拖入几百个文件后退出，
重启时只有最新的 200 条被重新调度，更早的那批停在 pending 状态且不在界面上，
成为死数据。改为按状态查询。

### 其他

- `knowledge_embeddings` 补 `model` 索引：语义检索按模型过滤全部分块，换过
  模型的库里旧向量仍在表中，此前只能全表扫描
- 清空 `config/file-line-limit-baseline.json`：7 条豁免里 5 条指向 PromptHub
  时期已删除的文件，剩下 2 条给最该拆分的两个文件留了各约 900 行的免检空间。
  检查脚本新增失效条目检测，豁免指向不存在的文件、或对应文件已缩到门槛以下时
  直接报错

## v0.4.1

### 修复：更新弹窗显示整份 CHANGELOG

版本标题的解析正则只认 `## [0.4.0]` 这种方括号写法，而本项目的 CHANGELOG
用的是 `## v0.4.0`，两条解析路径都匹配不到，最终落进「一个版本都没找到就返回
全文」的兜底分支。现在解析下沉到 `src/utils/changelog.ts`，同时兼容
`## v0.4.0`、`## 0.4.0` 与 `## [0.4.0]`，并补了单测。

打包时也不再把整份 CHANGELOG 写进 `latest.yml`——更新清单只带当前版本一节，
从 9.3 KB 降到 2.5 KB，且不会随版本数无限增长。

### 移除：WebDAV / S3 同步通道

fork 自 PromptHub 的同步管线一直是未接线的死代码——IPC handler、preload 白名单、
settings 里 36 个字段和 69 条 i18n 文案都在，但没有任何界面或服务引用它们，
也不存在把知识数据序列化上传的实现。整体删除：

- 主进程 `webdav.ts` / `s3.ts` 与注册调用、自动同步日志 handler
- preload 的 `window.electron.webdav` / `.s3` 及类型声明
- shared 的 `SyncSettings` / `SyncProviderKind` 等类型与 S3 IPC 频道常量
- 渲染进程 `settings-sync-actions.ts` 与相关归一化、持久化逻辑
- `@aws-sdk/client-s3`、`@smithy/node-http-handler` 依赖
- 设置页导航「数据同步」改名为「数据备份」

归知目前不提供多设备同步，跨设备请用备份文件或 Markdown 导出中转。

### 文档

- README 补上知识库卡片视图与列表视图的实际截图

## v0.4.0

首个公开发布版本。在 v0.3.0-alpha.1（内部预发布）的基础上补齐了数据安全、
语义检索、媒体采集与 Wiki 图谱，并重构了知识库界面。

### 知识库界面重构

- 双视图：卡片视图（虚拟化列表 + 常驻详情面板，中间栏宽度可拖拽）与
  列表视图（全宽表格，详情在全屏弹窗打开），视图选择持久化
- 表格列配置：标题 / 摘要 / 标签 / 类型 / 知识库 / 状态 / 创建时间 /
  更新时间 / 操作可显隐、可拖拽调宽，复选框列与操作列左右吸附；
  每页 10/20/50/100 条分页
- 标签选择器改为浮层：当前标签、新建标签、已有标签与 AI 建议合并在一处，
  详情页与列表右键菜单共用
- 范围切换改为带计数徽标的分段控件，回收站独立置于侧栏底部
- 详情头重整：标题、收藏 / 置顶 / 归档 / 回收站操作与元信息 chip
  （类型、知识库、来源、保存状态、字数）一行收敛；正文与文字稿分 Tab
- 顶栏「新建」改为拆分按钮：主按钮开快速采集，下拉提供空白笔记与导入文件
- 修复：界面多处写死 `Ctrl+N` 作为快速采集提示，但 Windows 下不注册应用菜单，
  该组合键从未生效；改为读取主进程实际生效的快捷键（默认 `Alt+Shift+N`）

### 媒体采集（M9）

- 本地媒体文件导入：图片 / 音频 / 视频拖入快速采集即资产化入库
  （`local-image://`、`local-video://` 协议服务），详情页内联预览与播放
- 图片 OCR：详情页「识别图中文字」，走 visionText 路由的多模态模型，
  结果作为「图中文字」小节写入正文（进入全文检索与语义索引）
- 音视频转写：新增 audioText 模型路由（AI 工作台第五条路由，指向
  whisper 类模型）；详情页「生成文字稿」经主进程 multipart 上传
  `/audio/transcriptions`，文字稿写入 transcript（问答与语义索引使用）
- 在线视频采集：B 站 / YouTube / 抖音 / 小红书链接自动识别，yt-dlp
  解析元数据并下载音轨（免 ffmpeg），配置转写模型后自动生成文字稿
- yt-dlp 应用内管理：设置页一键安装（官方源 + 镜像回退、走网络代理、
  实时进度条），自动校验可执行性后托管在应用数据目录；支持更新 /
  移除内置版；状态卡显示来源（内置 / 系统 PATH / 自定义路径）与版本，
  自定义路径带文件选择器；未安装时导入降级保存链接并引导一键安装
- 修复：core 配置层路由白名单缺 embedding，导致语义检索的路由分配
  在重启后丢失

### Wiki 关系图谱（M10）

- Wiki 新增图谱视图：页面为节点（主题 / 实体 / 概念三色），
  物化的页间链接为边；缩放显示标题，点击节点跳转页面
- 图谱按需懒加载（react-force-graph-2d），不影响启动体积

### 语义检索（M8）

- Embedding 语义索引：条目分块（滑动窗口 + 标题前缀）→ embedding API 嵌入 →
  L2 归一化向量以 Float32 BLOB 落库（`knowledge_embeddings` 表）
- 增量索引：内容哈希 + 模型双重判定，编辑 / 换模型自动重新嵌入；
  后台每 5 分钟静默补索引，导入完成后延时触发；Ask 侧栏显示索引进度并可手动执行
- 混合检索：AI 问答的检索改为「FTS 关键词 + 语义向量」RRF 融合，
  同义改写、跨语言问法可召回；embedding 未配置或调用失败时静默退化为纯 FTS
- 模型接入：复用 AI 工作台已有的 embedding 路由（openai 协议 /v1/embeddings、
  Gemini OpenAI 兼容层；Anthropic 无 embeddings API 明确报不支持）

### 数据安全（M7）

- 本地备份：手动「立即备份」+ 定时自动备份（间隔 / 保留数可配），
  `VACUUM INTO` 在线一致性副本落在 `%userData%/backups`
- 从备份恢复：完整性与表结构校验、导入任务运行守卫、
  恢复前自动保存当前数据快照，完成后应用自动重启
- 升级前快照：应用内更新安装前自动备份一次（pre-update）
- Markdown 批量导出：frontmatter 元信息（标题/标签/集合/来源/时间），
  按知识库分文件夹，可直接导入 Obsidian 等工具；导出不携带任何机密

### 打磨（M7）

- AI 问答会话持久化：会话列表（切换 / 删除 / 新对话）落库 SQLite，
  重启后恢复上次会话
- 知识列表批量多选：Ctrl/Cmd+点击、Shift+范围选择，
  批量移动集合 / 归档 / 回收站 / 恢复 / 彻底删除，Esc 退出
- ask / wiki / imports 三模块侧栏面板补齐（会话历史、编译状态 + 最近页面、
  任务概览 + 快速采集），替换占位文案
- 「自动保存」开关真正生效：关闭后显示未保存标记，用保存按钮或 Ctrl+S 落盘
- macOS 菜单「导入 / 导出」接通（快速采集 / 数据设置）
- e2e 冒烟测试：真实 Electron 启动 → 建条目 → 全文搜索 → 手动备份
- 清理：移除无引用的 SettingsModal 与 isDatabaseEmpty 空壳、
  过时的里程碑注释与占位词条

## v0.3.0-alpha.1

内部预发布，未公开分发。归知从 .NET/Avalonia 全面迁移到 Electron/React 技术栈的首个版本。
UI 与交互框架源自 [PromptHub](https://github.com/legeling/PromptHub) v0.5.9（AGPL-3.0），
在其应用骨架上重建归知的全部知识管理能力。

### 应用骨架（M0）

- 保留 PromptHub 应用骨架：窗口/托盘/快捷键、Liquid Glass 主题
  （12 套莫兰迪主题色、深浅色、背景图）、设置框架、AI Provider 工作台、
  WebDAV/S3 传输、更新器与构建发布链
- 移除 PromptHub 业务域（Prompt/Skill/MCP/Plugin/Rules/Agents/Cloud/CLI）
- 品牌化：归知图标、appId `com.couleurshare.guizhi`、数据目录
  `%APPDATA%/GuiZhi`（新库 `data/knowledge.db`，与旧 .NET 版数据并存）
- 语言精简为简体中文 / English

### 知识库（M1）

- 知识条目：Markdown 编辑（CodeMirror）/ 预览、防抖自动保存、
  收藏 / 置顶 / 归档 / 回收站
- 知识库（集合）与标签管理（六色标签、自动建标签）
- 中文全文检索：FTS5 按字分词 + 英文前缀匹配，相关性排序
- 三栏工作区：范围导航（收件箱/全部/收藏/归档/回收站）+ 虚拟化列表 + 详情

### 采集管线（M2）

- 快速采集（Ctrl+N / 托盘）：粘贴文本或链接自动识别、.txt/.md 文件选择与拖放、
  指定目标知识库、空白笔记直达
- 导入队列：并发 2、持久化、重启恢复、取消 / 重试、进度角标
- 网页抓取：Readability 正文抽取 + Markdown 转换、SSRF 防护、
  编码回退、抓取失败自动降级保存链接
- 去重：规范化 URL（剥离跟踪参数）+ 内容哈希双重判定，
  重复条目可打开已有或强制创建副本

### AI 能力（M3）

- AI 问答：Agent 工具循环（search / read / answer 动作协议，检索即推理）
  优先，协议失败自动回退单发 RAG；回答带可跳转引用，来源按实际标注对齐
- AI 摘要：短文单发、长文 map-reduce 分块，写入条目摘要卡片
- AI 标签建议：一键生成可点选标签
- 多协议模型支持（OpenAI 兼容 / Gemini / Anthropic），
  场景级模型路由（摘要/标签走快速模型，问答/Wiki 走主力模型）

### 知识 Wiki（M4）

- AI 把知识条目增量编译为互链的 Wiki 页面网络（主题 / 实体 / 概念）
- `[[链接]]` 白名单清洗（幻觉链接降级纯文本）、反向链接、来源条目回溯
- 增量指纹（素材哈希 + 提示词版本），自动编译开关（默认关）+
  手动立即编译 / 全量重建
- 问答检索融合 Wiki：页面优先命中，阅读页面时出链与来源成为新的检索线索

### 数据迁移（M5）

- 首次启动自动检测旧版（.NET）`guizhi.db`，一键迁入条目 / 知识库 / 标签 /
  来源记录 / Wiki 全量数据（含编译指纹，不触发重编）
- 在临时拷贝上执行（原文件只读不动），WAL 日志自动降级兼容

### 已知限制

- macOS 安装包暂缓（待签名证书；图标资产待替换）
- 本地 OCR / 语音转写延后（当前依赖 OpenAI 兼容远程 API）
- 中文平台（B 站 / 小红书 / 抖音）视频解析待后续版本恢复

历史版本（.NET/Avalonia 时代 v0.1.x–v0.2.x）见旧仓库的 GitHub Releases。
