/**
 * 文字稿 AI 排版的两个尺寸常量。
 *
 * 放在共享层是因为渲染进程也要用：长稿要在点击前弹确认框说清代价，
 * 而「多长算长」「大概几次请求」必须与主进程实际的切块口径一致。
 */

/** 单块上限：兼顾输出 token 限制与单请求耗时 */
export const TRANSCRIPT_FORMAT_CHUNK_CHARS = 1600;

/**
 * 自动排版的长度上限。超过它的文字稿在导入链路里跳过排版
 * （约 32 次串行请求，用户没主动要求时不该悄悄花掉），
 * 用户在详情页确认后可越过。
 */
export const TRANSCRIPT_FORMAT_LONG_CHARS = 50_000;
